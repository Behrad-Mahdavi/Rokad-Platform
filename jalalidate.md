# بریف فنی: معماری تقویم شمسی (جلالی) در پلتفرم رکاد

این سند مکمل `CLAUDE.md` است و دقیقاً مشخص می‌کند تقویم شمسی در کدام لایه‌ها، با چه ابزاری و طبق چه قواعدی در سراسر پلتفرم رکاد پیاده‌سازی می‌شود. هدف این است که هیچ توسعه‌دهنده یا دستیار هوش مصنوعی مجبور به تصمیم‌گیری موردی درباره تاریخ نباشد.

---

## ۱. اصل بنیادین: جداسازی «ذخیره‌سازی» از «نمایش»

> [!CRITICAL]
> **قانون طلایی:** در هیچ جدول یا کوئری‌ای، تاریخ شمسی به‌عنوان مقدار خام ذخیره نمی‌شود. دیتابیس فقط میلادی/UTC می‌شناسد؛ شمسی فقط در ورودی/خروجی API و در UI ظاهر می‌شود.

- **لایه دیتابیس (Postgres/Prisma):** همیشه `DateTime` استاندارد میلادی با UTC (`@default(now())`, `@db.Timestamptz`).
- **لایه دامنه/سرویس (Business Logic):** تمام محاسبات (تفاضل روز، مقایسه بازه، سورت) روی همان `Date` جاوااسکریپتی/میلادی انجام می‌شود.
- **لایه ارائه (API boundary):** تبدیل دوطرفه شمسی↔میلادی **فقط** در این لایه اتفاق می‌افتد — هنگام ورود (DTO) و هنگام خروج (Interceptor/Serializer).

دلیل: اگر تبدیل در وسط منطق تجاری پخش شود، باگ‌های کبیسه و مرزی (نگاه کنید به بخش ۷) در همه‌جا تکرار می‌شوند و تست‌پذیری از بین می‌رود.

---

## ۲. انتخاب کتابخانه

| نیاز | کتابخانه پیشنهادی | دلیل |
|---|---|---|
| تبدیل خالص شمسی↔میلادی (بدون وابستگی سنگین) | **`jalaali-js`** | سبک، بدون وابستگی، الگوریتم نجومی دقیق (نه سیکل ۳۳ ساله تقریبی)، مناسب لایه بک‌اند |
| فرمت‌دهی/پارس متن فارسی، اسم ماه، روز هفته | **`dayjs`** + پلاگین `jalaliday` | چون بقیه پروژه احتمالاً از `dayjs` برای عملیات عمومی تاریخ استفاده می‌کند؛ سبک‌تر از moment |
| اعتبارسنجی DTO ورودی (`class-validator`) | یک **Custom Validator** به نام `@IsJalaliDate()` ساخته‌شده روی `jalaali-js` | تا پیام خطا فارسی و دقیق باشد، نه خطای عمومی `class-validator` |

**تصمیم قطعی:** از `moment-jalaali` استفاده نشود (پروژه moment وارد حالت نگهداری صرف شده و باندل سنگینی دارد). ترکیب `jalaali-js` (منطق) + `dayjs/jalaliday` (نمایش) استاندارد پروژه است.

---

## ۳. لایه Prisma / دیتابیس

هیچ فیلد جدیدی برای «تاریخ شمسی» در schema اضافه نمی‌شود. تمام فیلدهای تاریخ به همان شکل فاز ۱ باقی می‌مانند:

```prisma
model AcademicYear {
  id         String   @id @default(cuid())
  tenantId   String
  startDate  DateTime @db.Timestamptz // میلادی، اما معادل ۱ مهر
  endDate    DateTime @db.Timestamptz // میلادی، اما معادل ۳۱ خرداد یا شروع سال بعد
  titleFa    String   // مثلاً "۱۴۰۴-۱۴۰۵" — این رشته، تنها استثنای ذخیره‌سازی متن شمسی است
  tenant     Tenant   @relation(fields: [tenantId], references: [id])

  @@index([tenantId, startDate])
}
```

**استثنای مجاز:** فیلدهایی مثل `titleFa` (برچسب نمایشی سال تحصیلی، مثل `"۱۴۰۴-۱۴۰۵"`) که صرفاً یک برچسب متنی‌اند و منطق تجاری روی آن‌ها کوئری نمی‌زند، می‌توانند رشته شمسی ذخیره کنند. اما هرگز مبنای مقایسه یا فیلتر قرار نمی‌گیرند.

---

## ۴. لایه API — ورودی (Request → DB)

### ۴.۱ فرمت ورودی استاندارد
هر جایی که کلاینت تاریخ شمسی می‌فرستد، فرمت رشته‌ای ثابت `YYYY-MM-DD` (اعداد لاتین، نه فارسی) پذیرفته می‌شود:

```typescript
export class CreateAttendanceDto {
  @IsJalaliDate({ message: 'تاریخ وارد شده معتبر نیست (فرمت صحیح: ۱۴۰۴-۰۷-۰۱)' })
  date: string; // ورودی: "1404-07-01"
}
```

### ۴.۲ تبدیل خودکار در Pipe
یک `JalaliToGregorianPipe` سراسری (یا Transformer در سطح DTO با `class-transformer`) رشته شمسی را قبل از رسیدن به سرویس، به `Date` میلادی تبدیل می‌کند. سرویس‌ها **هرگز** رشته شمسی نمی‌بینند.

```typescript
@Transform(({ value }) => jalaaliToGregorian(value))
date: Date;
```

---

## ۵. لایه API — خروجی (DB → Response)

طبق قالب پاسخ استاندارد در `CLAUDE.md` (بخش ۶)، هر فیلد `DateTime` خروجی باید هم مقدار ISO میلادی و هم معادل شمسی را داشته باشد تا فرانت مجبور به تبدیل سمت کلاینت نباشد:

```json
{
  "success": true,
  "data": {
    "date": {
      "gregorian": "2026-09-23T00:00:00.000Z",
      "jalali": "1405-07-01",
      "jalaliDisplay": "۱ مهر ۱۴۰۵"
    }
  }
}
```

این تبدیل در `TransformInterceptor` عمومی (که در `CLAUDE.md` بخش ۳ نیز تعریف شده) با یک `@JalaliDate()` decorator روی فیلدهای DTO خروجی اعمال می‌شود، نه دستی در هر کنترلر.

> [!IMPORTANT]
> کوئری، فیلتر (`WHERE`)، سورت (`ORDER BY`) و رنج‌های تاریخی (`gte`/`lte`) در Prisma **همیشه با معادل میلادی** انجام می‌شود. اگر فرانت بازه شمسی می‌فرستد (مثلاً «از ۱ مهر تا ۳۰ آبان»)، تبدیل به میلادی در همان لایه DTO ورودی (بخش ۴) رخ می‌دهد، نه در query builder.

---

## ۶. تقویم تحصیلی — منطق دامنه‌ای مبتنی بر شمسی

سال تحصیلی ایران بر پایه شمسی تعریف می‌شود (مهر تا خرداد)، نه سال میلادی. این یعنی:

- **`AcademicYear`:** شروع = ۱ مهر (معادل میلادی محاسبه‌شده)، پایان = پایان خرداد سال بعد. این مقادیر باید **per-tenant تنظیم‌پذیر** باشند (برخی مدارس/مقاطع تاریخ شروع متفاوت دارند).
- **`Term` / نیمسال:** بازه‌های داخل سال تحصیلی، با همان قاعده (تاریخ شمسی ورودی ادمین → ذخیره میلادی).
- **هفته تحصیلی:** روز شروع هفته کاری ایران **شنبه** است، نه یکشنبه. هر منطق مربوط به «هفته جاری»، «برنامه هفتگی» (فاز ۲) باید `weekStartsOn` را صریحاً شنبه تنظیم کند (در `dayjs` یا هر کتابخانه محاسبه هفته)، وگرنه پیش‌فرض کتابخانه‌ها (یکشنبه/دوشنبه) باگ تولید می‌کند.
- **سن دانش‌آموز/مقطع تحصیلی:** اگر منطقی مثل «حداقل سن ورود به پایه اول» وجود دارد، محاسبه سن باید روی تاریخ تولد میلادی انجام شود؛ فقط نمایش سن (در صورت نیاز) شمسی می‌شود.

---

## ۷. تعطیلات رسمی و تقویم رویدادها (Per-Tenant)

این بخش مستقیماً به فاز ۳ (`operations` — تقویم و رویدادها) و فاز ۷ (فیچرفلگ per-tenant) وصل است:

1. **جدول `Holiday` سراسری (Global):** تعطیلات رسمی کشور (نوروز، عاشورا، انقلاب و ...) که سالانه توسط کتابخانه یا سرویس خارجی (یا جدول دستی seed‌شده) به‌روزرسانی می‌شود. این جدول **مستقل از tenant** است چون تعطیلات رسمی برای همه مدارس یکسان است.
   ```prisma
   model OfficialHoliday {
     id     String   @id @default(cuid())
     date   DateTime @db.Date // میلادیِ معادل، محاسبه‌شده یک‌بار در seed سالانه
     titleFa String
     @@unique([date])
   }
   ```
2. **جدول `TenantHoliday` (per-tenant):** تعطیلات اختصاصی هر مدرسه (روز تأسیس مدرسه، تعطیلی محلی، بازدید) — این‌ها دارای `tenantId` هستند و طبق «قوانین طلایی چندمستأجری» در `CLAUDE.md` باید ایندکس ترکیبی `[tenantId, date]` داشته باشند.
3. **جمعه‌ها:** به‌صورت الگوریتمی (نه رکورد دیتابیس) از روی `dayOfWeek` میلادی تشخیص داده می‌شوند (`getDay() === 5`)، چون تکرارشونده‌اند و ذخیره‌سازی آن‌ها به‌عنوان رکورد بی‌مورد است.
4. **محاسبه «روز کاری» برای حضور و غیاب (فاز ۳):** سرویس مرکزی `SchoolCalendarService` باید یک تابع واحد `isWorkingDay(tenantId, date)` ارائه دهد که هر سه منبع بالا (جمعه + Holiday سراسری + TenantHoliday) را ترکیب کند. **هیچ ماژول دیگری نباید این منطق را خودش بازسازی کند.**

---

## ۸. نکات فنی حساس و لبه‌ای (Edge Cases)

| مورد | ریسک | راه‌حل |
|---|---|---|
| **الگوریتم کبیسه شمسی** | سال کبیسه شمسی با فرمول نجومی دقیق تعیین می‌شود، نه سیکل ثابت ۳۳ ساله (که در بازه‌های دور خطا می‌دهد) | استفاده از `jalaali-js` که الگوریتم `jalaali-js:isLeapJalaaliYear` مبتنی بر محاسبه نجومی است، نه جدول تقریبی |
| **اسفند ۲۹ یا ۳۰ روزه** | تبدیل نادرست باعث off-by-one در پایان سال می‌شود | همیشه از تابع کتابخانه برای گرفتن تعداد روزهای ماه استفاده شود؛ هیچ‌جا عدد ثابت `29`/`30`/`31` هاردکد نشود |
| **مرز نیمه‌شب و Timezone** | چون DB به‌صورت UTC ذخیره می‌شود، یک تاریخ شمسی که در `Asia/Tehran` هنوز «امروز» است، ممکن است در UTC به روز قبل/بعد سُر بخورد | تمام تبدیل‌های شمسی باید ابتدا timestamp را به `Asia/Tehran` نرمال‌سازی کنند، سپس به شمسی تبدیل شوند (نه تبدیل مستقیم از UTC خام) |
| **اعداد فارسی در ورودی** | کاربر ممکن است تاریخ را با اعداد فارسی (`۱۴۰۴`) به‌جای لاتین وارد کند | `@IsJalaliDate()` باید قبل از اعتبارسنجی، اعداد فارسی/عربی را به لاتین نرمال‌سازی کند (تابع کمکی مشترک `normalizePersianDigits`) |
| **تفاوت سال تحصیلی مدارس دوزبانه/بین‌المللی** | برخی مدارس ممکن است تقویم میلادی بخواهند (فیچرفلگ آینده) | `calendarSystem` را از همین فاز به‌عنوان یک فیلد قابل‌توسعه (نه هاردکد) روی `Tenant` در نظر بگیرید، حتی اگر فعلاً فقط مقدار `JALALI` پشتیبانی شود |
| **مرتب‌سازی رشته‌ای شمسی** | اگر جایی به اشتباه `titleFa` یا رشته شمسی به‌صورت متنی سورت شود، نتیجه غلط است (`"1404-2"` قبل از `"1404-10"` می‌آید) | سورت همیشه روی معادل میلادی؛ رشته شمسی فقط برای نمایش |

---

## ۹. جمع‌بندی — چک‌لیست پیاده‌سازی

- [ ] نصب `jalaali-js` و `dayjs` + `jalaliday` در سطح مخزن مشترک (`common/`)
- [ ] پیاده‌سازی `@IsJalaliDate()` decorator در `common/decorators`
- [ ] پیاده‌سازی `@JalaliDate()` output decorator + بروزرسانی `TransformInterceptor`
- [ ] ساخت `SchoolCalendarService` با متد مرکزی `isWorkingDay()`
- [ ] مدل‌های `OfficialHoliday` (سراسری) و `TenantHoliday` (per-tenant) در schema
- [ ] تنظیم `weekStartsOn: 6` (شنبه) در تمام محاسبات هفته
- [ ] نرمال‌سازی timezone به `Asia/Tehran` قبل از هر تبدیل شمسی
- [ ] seed سالانه تعطیلات رسمی (اسکریپت یا Cron با BullMQ)
- [ ] تست واحد مخصوص edge caseهای بخش ۸ (کبیسه، اسفند، مرز نیمه‌شب)

این چک‌لیست باید در فاز ۱ (زیرساخت) تکمیل شود تا فازهای ۲ تا ۷ — که همگی به‌نوعی تاریخ تولید می‌کنند (سال تحصیلی، حضور، آزمون، تقویم رویداد، شهریه) — از یک منبع واحد و آزمایش‌شده استفاده کنند.