# بریف — توسعه ماژول شهریه: تخصیص گروهی، ثبت نقدی/چک، ایمپورت اکسل و یادآوری سررسید

## ۱. خلاصه نیاز

ماژول `FeeService` (فاز ۶) که فعلاً فقط شهریه + درگاه زرین‌پال داره، باید گسترش پیدا کنه به:
1. **تخصیص شهریه در سطح سال تحصیلی** — یک مبلغ/طرح شهریه تعریف شه و به‌صورت گروهی به همه دانش‌آموزان (یا یک مقطع/کلاس خاص) اختصاص پیدا کنه؛ همه اون‌ها بلافاصله بدهکار می‌شن.
2. **ثبت موردی شهریه** — امکان ثبت شهریه اختصاصی/استثنا برای یک دانش‌آموز خاص (خارج از تخصیص گروهی).
3. **پرداخت نقدی و چک** — علاوه بر درگاه آنلاین، امکان ثبت دستی پرداخت نقدی و چک، هرکدوم با جزئیات مخصوص خودش.
4. **ورود اطلاعات از اکسل** — هم برای تخصیص گروهی شهریه، هم برای ثبت گروهی پرداخت‌ها (نقدی/چک).
5. **یادآوری سررسید چک به والدین** — اطلاع‌رسانی خودکار قبل و در روز سررسید هر چک.
6. **دسترسی:** بخش شهریه/پرداخت فقط برای **والدین** قابل مشاهده‌ست (نه دانش‌آموز)؛ مدیریت کامل (تخصیص، ثبت نقدی/چک، اقساط) فقط برای **مدیر مدرسه/حسابدار**.

---

## ۲. مدل داده (افزوده به `schema.prisma` فاز ۶)

### ۲.۱ تخصیص شهریه — گروهی و موردی

```prisma
model FeePlan {
  id              String   @id @default(uuid())
  tenantId        String
  academicYearId  String
  title           String              // «شهریه سالانه پایه دهم ۱۴۰۴-۱۴۰۵»
  amount          Decimal
  appliesTo       FeePlanScope        // ALL_SCHOOL | EDUCATIONAL_LEVEL | CLASSROOM | INDIVIDUAL
  educationalLevelId String?
  classroomId     String?
  createdAt       DateTime @default(now())

  contracts       FeeContract[]
}

enum FeePlanScope {
  ALL_SCHOOL
  EDUCATIONAL_LEVEL
  CLASSROOM
  INDIVIDUAL
}

// FeeContract از فاز ۶ موجوده؛ فیلد اضافه می‌شه:
model FeeContract {
  // ... فیلدهای موجود (studentId, totalAmount, discountAmount, ...)
  feePlanId       String?             // اگه از تخصیص گروهی اومده باشه
  isIndividual    Boolean  @default(false)  // ثبت موردی/استثنا
  balanceRemaining Decimal            // محاسبه‌شده: totalAmount - مجموع پرداخت‌های تأییدشده
}
```

### ۲.۲ پرداخت — گسترش به سه روش

```prisma
enum PaymentMethod {
  ONLINE_GATEWAY   // همون Zarinpal فاز ۶
  CASH
  CHECK
}

enum CheckStatus {
  PENDING    // هنوز سررسید نرسیده / وصول نشده
  CASHED     // وصول شد (بدهی کاهش پیدا می‌کنه)
  BOUNCED    // برگشت خورد (بدهی برنمی‌گرده کم بشه)
  REPLACED   // با یک چک جدید جایگزین شد
}

model FeePayment {
  id             String        @id @default(uuid())
  tenantId       String
  feeContractId  String
  feeContract    FeeContract   @relation(fields: [feeContractId], references: [id])
  installmentId  String?       // اگه به یک قسط خاص مرتبطه
  method         PaymentMethod
  amount         Decimal
  recordedBy     String        // userId کاربری که ثبت کرده (حسابدار/مدیر)
  recordedAt     DateTime      @default(now())
  note           String?

  // فیلدهای اختصاصی نقدی
  cashReceivedAt DateTime?

  // فیلدهای اختصاصی چک
  checkNumber    String?
  checkSayadId   String?       // کد صیادی ۱۶ رقمی (استاندارد جدید چک در ایران)
  bankName       String?
  branchName     String?
  checkOwnerName String?       // اگه صاحب چک با پرداخت‌کننده فرق داره
  checkDueDate   DateTime?
  checkStatus    CheckStatus?
  checkStatusChangedAt DateTime?

  // ارجاع به تراکنش درگاه (فقط برای ONLINE_GATEWAY)
  gatewayTransactionId String?  // FK به PaymentTransaction فاز ۶

  @@index([tenantId, feeContractId])
  @@index([tenantId, checkDueDate, checkStatus])   // برای کوئری یادآوری سررسید
}
```

**نکته مهم محاسباتی:** `balanceRemaining` روی `FeeContract` فقط با پرداخت‌های `CASH` (تأییدشده)، `CHECK` با وضعیت `CASHED`، و `ONLINE_GATEWAY` verify‌شده کاهش پیدا می‌کنه. چک با وضعیت `PENDING` یا `BOUNCED` **بدهی رو کم نمی‌کنه** — این خیلی مهمه چون وگرنه یک چک برگشتی باعث می‌شه سیستم فکر کنه دانش‌آموز بدهی نداره در حالی که هنوز داره.

---

## ۳. جریان‌های اصلی (User Flows)

### ۳.۱ تخصیص گروهی شهریه
```
مدیر/حسابدار وارد «شهریه» می‌شه
  → «تعریف طرح شهریه جدید»
  → انتخاب سال تحصیلی + مبلغ + محدوده (کل مدرسه / یک مقطع / یک کلاس)
  → پیش‌نمایش: «این طرح روی X دانش‌آموز اعمال می‌شه»
  → تأیید نهایی
  → سیستم برای هرکدوم یک FeeContract می‌سازه (با احترام به تخفیف/بورسیه فردی که در فاز ۶ داشتیم)
  → همه دانش‌آموزان مربوطه بلافاصله بدهکار می‌شن
```

### ۳.۲ ثبت موردی (استثنا)
یک فرم ساده‌تر — انتخاب یک دانش‌آموز خاص + مبلغ اختصاصی (بدون وابستگی به `FeePlan` گروهی) → `FeeContract` با `isIndividual: true`.

### ۳.۳ ثبت پرداخت نقدی
فرم: انتخاب دانش‌آموز/قرارداد → مبلغ → تاریخ دریافت → یادداشت اختیاری → ثبت. بلافاصله `balanceRemaining` کاهش پیدا می‌کنه.

### ۳.۴ ثبت چک
فرم: انتخاب دانش‌آموز/قرارداد → مبلغ → شماره چک → **کد صیادی** → نام بانک/شعبه → نام صاحب چک (اگه متفاوته) → تاریخ سررسید → ثبت با وضعیت پیش‌فرض `PENDING`.
- در تاریخ سررسید (یا بعدش)، حسابدار وضعیت رو به‌صورت دستی به‌روزرسانی می‌کنه: «وصول شد» یا «برگشت خورد».
- اگه برگشت خورد، یک اعلان داخلی برای مدیر/حسابدار (نه فقط toast) باید ثبت بشه، چون این یک ریسک مالیه که نیاز به پیگیری داره.

---

## ۴. ورود اطلاعات از اکسل (Excel Import)

### ۴.۱ دو نوع Import مجزا

**الف) Import تخصیص شهریه:**
| ستون‌های Excel | توضیح |
|---|---|
| کد ملی یا شماره دانش‌آموزی | شناسایی دانش‌آموز |
| مبلغ شهریه | |
| تخفیف (اختیاری) | |
| توضیحات (اختیاری) | |

**ب) Import پرداخت‌ها (نقدی/چک):**
| ستون‌های Excel | توضیح |
|---|---|
| کد ملی یا شماره دانش‌آموزی | |
| نوع پرداخت | `نقدی` یا `چک` |
| مبلغ | |
| تاریخ (دریافت نقدی یا سررسید چک) | |
| شماره چک (اگه نوع=چک) | |
| کد صیادی (اگه نوع=چک) | |
| نام بانک (اگه نوع=چک) | |

### ۴.۲ فرآیند و اعتبارسنجی

```
آپلود فایل اکسل (با استفاده از SheetJS که در استک فرانت داریم)
  → Parse و نمایش پیش‌نمایش جدول (قبل از ثبت نهایی — خیلی مهم، کاربر باید ببینه چی می‌خواد ثبت کنه)
  → اعتبارسنجی هر ردیف:
     - آیا دانش‌آموز با این کد ملی/شماره وجود داره؟
     - آیا مبلغ عدد معتبره؟
     - آیا کد صیادی ۱۶ رقمیه (اگه نوع چک)؟
  → نمایش نتیجه: «۴۵ ردیف معتبر / ۳ ردیف با خطا» + لیست دقیق خطاهای هر ردیف (با شماره ردیف)
  → دکمه «ثبت فقط ردیف‌های معتبر» یا «اصلاح و آپلود دوباره»
```

**نکته فنی مهم:** Parse فایل اکسل باید **سمت سرور** انجام بشه (نه فقط پیش‌نمایش سمت کلاینت با SheetJS)، چون داده‌های مالی گروهی نباید فقط با اعتبارسنجی کلاینتی وارد سیستم بشن — کلاینت فقط برای پیش‌نمایش سریع خوبه.

---

## ۵. یادآوری سررسید چک به والدین

### ۵.۱ منطق زمان‌بندی
یک Job دوره‌ای (روزانه، با BullMQ که از فاز ۵ داریم) که:
```sql
SELECT * FROM FeePayment
WHERE method = 'CHECK'
  AND checkStatus = 'PENDING'
  AND checkDueDate IN (today + 3 days, today + 1 day, today)
```
و برای هرکدوم یک اعلان (از `NotificationModule`) به والدینِ اون دانش‌آموز می‌فرسته (SMS + اعلان درون‌برنامه‌ای، طبق چیزی که در بریف توست هم گفتیم — نوع Info).

### ۵.۲ متن نمونه اعلان
- ۳ روز قبل: «چک شما به شماره {شماره چک} به مبلغ {مبلغ} تومان، {۳} روز دیگه سررسید می‌شه.»
- روز سررسید: «امروز سررسید چک شماره {شماره چک} به مبلغ {مبلغ} تومانه. لطفاً از کافی بودن موجودی حساب مطمئن شوید.»

### ۵.۳ نکته
این یادآوری برای **والدین** (صاحب حساب/چک) می‌ره، نه دانش‌آموز — چون طبق چیزی که خودت گفتی، بخش شهریه فقط مال والدینه.

---

## ۶. دسترسی‌ها (بازتاب در RBAC موجود)

| نقش | دسترسی |
|---|---|
| **دانش‌آموز** | هیچ دسترسی به بخش شهریه/پرداخت (نه مشاهده، نه پرداخت) |
| **والدین** | مشاهده وضعیت بدهی فرزند، تاریخچه پرداخت‌ها، لیست چک‌های ثبت‌شده و سررسیدشون؛ امکان پرداخت آنلاین (زرین‌پال) برای مانده بدهی |
| **حسابدار/مدیر (`finance.write`)** | تخصیص گروهی/موردی شهریه، ثبت نقدی/چک، تغییر وضعیت چک، Import اکسل، مشاهده گزارش کامل مالی همه دانش‌آموزان |

---

## ۷. نکات امنیتی/صحت (هم‌راستا با اصولی که در فاز ۶ رعایت کردیم)

1. **Audit Log برای هر ثبت/تغییر دستی** — چون این‌ها برخلاف پرداخت آنلاین، ورودی انسانیه و امکان خطا/تقلب داره؛ هر ثبت نقدی/چک و هر تغییر وضعیت چک باید با `recordedBy` و در `AuditLog` ثبت بشه.
2. **اعتبارسنجی مبلغ:** مجموع پرداخت‌های ثبت‌شده روی یک قرارداد نباید از `totalAmount` بیشتر بشه (مگه با یک پرچم صریح «پرداخت اضافه» که نیاز به تأیید مدیر داره).
3. **Permission جدا برای تغییر وضعیت چک** — پیشنهاد می‌کنم `finance.check.manage` جدا از `finance.write` عمومی باشه، چون تغییر وضعیت چک (وصول/برگشت) خودش یک اقدام حساس مالیه که شاید نخوای هر کسی با `finance.write` بتونه انجامش بده.
4. **جلوگیری از Double-Entry در Import اکسل:** اگه یک ردیف اکسل دقیقاً با یک پرداخت قبلاً ثبت‌شده (همون دانش‌آموز، همون مبلغ، همون تاریخ) یکسان باشه، سیستم باید هشدار «احتمال تکراری» بده، نه بی‌صدا دوباره ثبت کنه.

---

## ۸. سوالات باقی‌مانده برای تکمیل پلن

- آیا امکان **اقساط‌بندی روی تخصیص گروهی** هم لازمه (مثلاً شهریه در ۶ قسط ماهانه تقسیم بشه)، یا فعلاً `FeeContract` گروهی به‌صورت یک‌جا (Lump Sum) کافیه و اقساط فقط برای موردی؟
- برای «برگشت خوردن چک»، آیا فقط یک اعلان داخلی کافیه یا باید یک وضعیت خاص (مثلاً «هشدار مالی») روی پروفایل دانش‌آموز/والدین هم ثبت بشه که در جاهای دیگه سیستم (مثلاً کارنامه یا گزارش‌ها) هم دیده بشه؟