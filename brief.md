# بریف کامل پروژه: پلتفرم SaaS چندمستأجری مدیریت مدرسه (School ERP/LMS)

## ۱. معرفی و هدف پروژه

هدف، ساخت یک پلتفرم **SaaS چندمستأجری (Multi-Tenant)** برای مدیریت کامل یک مدرسه/آموزشگاه است که شامل سه لایه اصلی می‌شود:

1. **ERP آموزشی** — ساختار سازمانی، دانش‌آموز، معلم، کلاس، حضور و غیاب، مالی و منابع انسانی
2. **LMS (سیستم مدیریت یادگیری)** — بانک سوال، آزمون، محتوا، کلاس آنلاین
3. **پلتفرم SaaS** — مدیریت چند مدرسه (tenant) روی یک زیرساخت مشترک، با دامنه/ساب‌دامین اختصاصی، فیچرفلگ و صورت‌حساب هر مدرسه

نکته کلیدی پروژه شماست: **ماژول‌ها به‌صورت لایه‌ای و وابسته به هم توسعه داده می‌شوند** — هر فاز روی زیرساخت فاز قبل سوار می‌شود، پس ترتیب توسعه اختیاری نیست، اجباری است.

## ۲. منطق وابستگی بین فازها

قبل از ترتیب، دلیل این چیدمان را ببینیم — چون همین منطق تعیین می‌کند چرا نمی‌شود مثلاً فاز ۴ (آزمون) را قبل از فاز ۲ (دانش‌آموز/کلاس) ساخت:

- هیچ ماژولی بدون **چندمستأجری بودن** (tenant_id در همه جداول) و **Auth/RBAC** معنا ندارد → این‌ها زیربنای مطلق‌اند.
- **ساختار آموزشی** (سال تحصیلی، پایه، کلاس، دانش‌آموز، معلم) پیش‌نیاز هر داده عملیاتی دیگری‌ست — حضور و غیاب، تکلیف، نمره، همه به یک دانش‌آموز در یک کلاس مشخص وصل می‌شوند.
- **عملیات روزمره** (حضور، تکلیف، تقویم) روی ساختار آموزشی سوار می‌شود.
- **موتور ارزیابی/LMS** (بانک سوال، آزمون، نمره) به دانش‌آموز و کلاس نیاز دارد، پس بعد از فاز ۲ و ۳ می‌آید.
- **ارتباطات و محتوای زنده** (چت، کلاس آنلاین، اعلان) روی هویت کاربران (فاز ۱-۲) و محتوای درسی (فاز ۴) سوار است.
- **مالی و منابع انسانی** به ساختار کلاس/دانش‌آموز/معلم برای محاسبه شهریه و حقوق نیاز دارد، و معمولاً از نظر ریسک و پیچیدگی، دیرتر توسعه داده می‌شود تا مدل داده‌ای پایدار شده باشد.
- **پنل سوپرادمین SaaS** آخرین لایه است چون خودش مصرف‌کننده همه ماژول‌های قبلی است (فیچرفلگ را به تننت اختصاص می‌دهد، تننت‌ها را onboard می‌کند، و روی همه‌چیز audit log می‌گذارد).

## ۳. ترتیب دقیق توسعه (Roadmap فازبندی‌شده)

### فاز ۱ — پایه و چندمستأجری (Foundation & Multi-Tenancy)
**چرا اول:** بدون این فاز هیچ خط کدی در فازهای بعد قابل نوشتن نیست.
- مستندسازی پروژه (Claude.md / معماری، قراردادهای کد)
- انتخاب/استقرار ORM و طراحی الگوی چندمستأجری در دیتابیس (schema-per-tenant یا shared-schema با tenant_id — این تصمیم روی همه فازهای بعدی اثر می‌گذارد)
- سیستم Feature Flag (چون فاز ۷ به آن وابسته است، باید از روز اول قابل‌توسعه باشد)
- Auth (JWT/Session + Multi-tenant aware login)

**خروجی فاز:** یک اسکلت بک‌اند که می‌تواند چند تننت را جدا از هم سرویس دهد و هر تننت بتواند لاگین کند.

---

### فاز ۲ — هسته ERP و ساختار سازمانی (Core ERP & Structure)
**چرا دوم:** این فاز «واژه‌نامه» کل سیستم را می‌سازد — همه فازهای بعدی به این جداول ارجاع می‌دهند.
- سال تحصیلی، ترم، مقطع تحصیلی، رشته
- درس، کلاس، برنامه کلاسی
- RBAC (نقش‌ها/دسترسی‌های سطح مدرسه — جدا از سوپرادمین)
- دانش‌آموز، معلم، مربی (Coach)، پرسنل، والدین + پیوند والد-دانش‌آموز
- پروفایل کاربر، بلاگ پروفایل، پروفایل مدرسه

**نکته توسعه:** RBAC را همین‌جا محکم بسازید؛ چون هم فاز ۳ (دسترسی به حضور/تکلیف) و هم فاز ۶ (دسترسی مالی) رویش سوار می‌شوند.

**خروجی فاز:** یک مدرسه کامل با ساختار سازمانی واقعی قابل تعریف است (کلاس‌بندی، افراد، نقش‌ها).

---

### فاز ۳ — عملیات روزمره آموزشی (Daily Academic Operations)
**چرا سوم:** اولین لایه‌ای‌ست که «رویداد روزانه» تولید می‌کند و باید روی ساختار فاز ۲ بنشیند.
- حضور و غیاب، تکلیف
- تقویم، رویدادها، نظرسنجی
- ملاقات والدین (Parents Visit)، گزارش‌ها، مسائل انضباطی/اداری (Matters)

**خروجی فاز:** مدرسه می‌تواند یک روز کاری واقعی را در سیستم ثبت کند (حضور بزند، تکلیف بدهد، گزارش بگیرد).

---

### فاز ۴ — ارزیابی و هسته LMS (Assessment & LMS Core)
**چرا چهارم:** آزمون و نمره بدون دانش‌آموز/کلاس (فاز ۲) و بدون تقویم/برنامه (فاز ۳) بی‌معناست.
- بانک سوال
- موتور آزمون (Exam Engine)
- نمرات
- طرح درس (Lesson Plan)

**خروجی فاز:** معلم می‌تواند آزمون بسازد، دانش‌آموز شرکت کند، نمره ثبت و گزارش شود.

---

### فاز ۵ — ارتباطات و محتوای زنده (Communication & Live Content — LMS بخش دوم)
**چرا پنجم:** نیازمند هویت کاربران (فاز ۱-۲) و محتوای درسی (فاز ۴) به‌عنوان چیزی برای اشتراک‌گذاری/بحث است. این فاز از نظر فنی سنگین‌ترین بخش (real-time) پروژه است، پس بهتر است روی یک مدل داده پایدار پیاده شود.
- مدیریت محتوا (ساخت/مشاهده محتوا + ذخیره‌سازی MinIO/S3)
- کلاس آنلاین (Wrapper و اتصال به BBB یا Jitsi)
- سیستم پیام‌رسانی (WebSocket + Redis Pub/Sub + چت درون‌برنامه‌ای)
- موتور اعلان و پیامک (SMS درون‌برنامه‌ای + Event Dispatcher)

**پیشنهاد فنی:** Event Dispatcher همین‌جا به‌صورت عمومی (generic) طراحی شود تا فاز ۶ (رسید پرداخت، اعلان فیش حقوقی) و فاز ۷ (اعلان به تننت) هم از همان زیرساخت استفاده کنند، نه اینکه دوباره‌سازی شود.

**خروجی فاز:** کلاس آنلاین برگزار می‌شود، محتوا آپلود/مشاهده می‌شود، اعلان و چت زنده کار می‌کند.

---

### فاز ۶ — مالی و منابع انسانی (Finance & HR Operations)
**چرا ششم:** محاسبه شهریه به دانش‌آموز/کلاس (فاز ۲) و محاسبه حقوق به معلم/پرسنل (فاز ۲) نیاز دارد؛ رسیدها و اعلان پرداخت هم از زیرساخت اعلان فاز ۵ استفاده می‌کنند.
- مدیریت شهریه و اقساط (Fee Engine، Installments، رسید/گزارش شهریه)
- موتور حقوق و دستمزد (حقوق معلم، پرسنل، سوابق پرسنل مستعفی/اخراجی)
- **سیستم کا و تنظیمات مدرسه** — طبق توضیح شما، این ماژول از قبل توسعه یافته و **فقط لایه API آن باید نوشته شود** (بدون نیاز به بازطراحی UI یا منطق موجود). این یعنی در این فاز، تیم بک‌اند صرفاً روی API-ایز کردن سیستم کا و تنظیمات عمومی تمرکز می‌کند، نه توسعه از صفر.

**خروجی فاز:** چرخه مالی کامل مدرسه (شهریه تا حقوق) عملیاتی می‌شود.

---

### فاز ۷ — سوپرادمین SaaS و عملیات پلتفرم (SaaS Super Admin & Platform Operations)
**چرا آخر:** این لایه، مدیر همه‌چیزهای بالاست — تننت‌سازی، تخصیص فیچرفلگ، ساخت نقش پویا، و لاگ کل سیستم. فقط وقتی معنا دارد که حداقل یک مدرسه واقعی (فازهای ۱ تا ۶) کامل روی سیستم پیاده شده باشد تا بتوان رفتار آن را مدیریت/مانیتور کرد.
- مدیریت تننت (Onboarding مدرسه، Provisioning ساب‌دامین/دامین)
- مدیریت اشتراک و ماژول (تخصیص Feature Flag به هر تننت — این یعنی سیستم Feature Flag فاز ۱ باید از اول قابل‌تخصیص per-tenant طراحی شده باشد)
- سازنده نقش (Dynamic Permission Builder — نسخه سوپرادمین RBAC فاز ۲)
- برندها، کالج‌ها، پروژه‌ها، اعضای باشگاه (Club Members)
- ابزارهای پلتفرم: مدیریت فایل، پشتیبان‌گیری دیتابیس، لاگ‌های ممیزی سیستم

**خروجی فاز:** پلتفرم آماده فروش SaaS به مدارس متعدد است؛ هر مدرسه جدید در چند دقیقه onboard می‌شود.

## ۴. جدول خلاصه ترتیب توسعه

| فاز | نام | پیش‌نیاز مستقیم | ریسک فنی اصلی |
|---|---|---|---|
| ۱ | پایه و چندمستأجری | — | تصمیم معماری چندمستأجری (schema/shared) غیرقابل‌بازگشت آسان است |
| ۲ | هسته ERP | فاز ۱ | طراحی درست RBAC از ابتدا |
| ۳ | عملیات روزانه | فاز ۲ | حجم داده روزانه بالا (حضور/تکلیف) |
| ۴ | ارزیابی و LMS | فاز ۲، ۳ | موتور آزمون همزمان (concurrency) در امتحانات آنلاین |
| ۵ | ارتباطات زنده | فاز ۱، ۲، ۴ | Real-time (WebSocket/Redis) و اتصال BBB/Jitsi |
| ۶ | مالی و HR | فاز ۲، ۵ | صحت محاسباتی (شهریه/حقوق) + فقط API برای سیستم کا |
| ۷ | سوپرادمین SaaS | همه فازها | ایزوله‌سازی امنیتی کامل بین تننت‌ها |

## ۵. توصیه‌های عملیاتی برای اجرا

- **موازی‌کاری محدود:** فاز ۱ و ۲ باید کاملاً سریال باشند؛ اما از فاز ۳ به بعد، اگر تیم بزرگ‌تر است، می‌توان زیرماژول‌های مستقل را موازی برد (مثلاً «تقویم/رویداد» با «حضور/تکلیف» هم‌زمان توسعه یابند چون فقط به فاز ۲ وابسته‌اند، نه به هم).
- **Event Dispatcher عمومی:** از همان فاز ۵ به‌گونه‌ای بسازید که فازهای ۶ و ۷ بتوانند مصرف‌کننده آن باشند (اعلان رسید شهریه، اعلان onboarding تننت جدید).
- **Feature Flag از روز اول per-tenant:** چون فاز ۷ (تخصیص ماژول به تننت) کاملاً به این وابسته است، طراحی اولیه در فاز ۱ باید از همان ابتدا «تخصیص‌پذیر per-tenant» باشد، نه یک flag سراسری ساده.
- **سیستم کا:** چون منطق/UI آماده است، پیشنهاد می‌شود یک قرارداد API (OpenAPI/Swagger) از رفتار فعلی سیستم کا استخراج شود تا توسعه API بدون نیاز به تفسیر مجدد منطق تجاری انجام شود.
- **مهاجرت داده و تست:** برای هر فاز، قبل از رفتن به فاز بعد، یک تننت آزمایشی با داده واقعی/شبیه‌سازی‌شده پر شود تا مشکلات مدل داده زودتر از فاز‌های وابسته بعدی کشف شوند.

## ۶. پشته فناوری پیشنهادی (Technology Stack)

| لایه / بخش | فناوری پیشنهادی | دلیل انتخاب و توضیحات فنی |
|---|---|---|
| **بک‌اند** | Node.js + NestJS (TypeScript) | ساختار ماژولار NestJS دقیقاً با این معماری فازبندی‌شده همخوانی دارد؛ DI و Guard برای پیاده‌سازی RBAC و Multi-tenancy عالی است. |
| **دیتابیس** | PostgreSQL | پشتیبانی قوی از Row-Level Security (برای ایزوله‌سازی تننت‌ها) + JSONB برای داده‌های نیمه‌ساخت‌یافته |
| **ORM** | Prisma یا TypeORM | Prisma برای migration و type-safety بهتر است؛ TypeORM اگر به الگوهای پیچیده‌تر Repository نیاز باشد. |
| **کش / پیام‌رسانی داخلی** | Redis | هم برای Pub/Sub (چت، اعلان)، هم برای Cache و هم زیرساخت صف‌ها (BullMQ) |
| **صف کار (Queue)** | BullMQ (روی Redis) | برای Event Dispatcher، ارسال پیامک/اعلان ناهمگام (Async) و پردازش گزارش‌های سنگین |
| **ذخیره‌سازی فایل** | MinIO (self-hosted) یا S3 | ذخیره‌سازی شیءمحور (S3-compatible) برای اسناد، فایل‌ها و محتوای آموزشی طبق دیاگرام معماری |
| **ارتباط بلادرنگ (Real-time)** | Socket.IO یا WebSocket خام + Redis Adapter | برای چت درون‌برنامه‌ای و سیستم اعلان زنده |
| **کلاس آنلاین** | Jitsi (self-hosted، سبک‌تر) یا BigBlueButton (امکانات کامل‌تر LMS) | Jitsi برای شروع سریع‌تر و هزینه کمتر؛ BBB اگر ضبط کلاس و Breakout Room حیاتی باشد. |
| **فرانت‌اند** | Next.js (React) | SSR برای صفحات عمومی (پروفایل مدرسه، لندینگ تننت) + SPA برای پنل‌های مدیریتی و کاربری |
| **احراز هویت (Auth)** | JWT + Refresh Token، یا Auth.js با فرانت Next.js | طراحی Multi-tenant aware از ابتدا (حضور `tenant_id` در Payload توکن) |
| **مدیریت قابلیت‌ها (Feature Flag)** | Unleash (self-hosted) یا پیاده‌سازی داخلی روی Postgres/Redis | باید از روز اول per-tenant قابل تخصیص باشد (پیش‌نیاز فاز ۷) |
| **زیرساخت و استقرار** | Docker + Docker Compose (شروع) → Kubernetes (مقیاس بالا) | توسعه و استقرار استاندارد کانتینری؛ ارتقا به K8s با افزایش تعداد مدارس/تننت‌ها |
| **مانیتورینگ و لاگ** | Grafana + Loki یا ELK Stack | جمع‌آوری لاگ‌ها، پایش سلامت سیستم و ممیزی (System Audit Logs در فاز ۷) |