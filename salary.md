# بریف — توسعه ماژول حقوق و دستمزد: قرارداد، محاسبه خودکار، ویرایش نهایی و خروجی‌ها

## ۱. خلاصه نیاز

`PayrollService` (فاز ۶) باید به یک چرخه کامل و خودکار تبدیل بشه:
1. **اول سال تحصیلی:** مدیر برای هر مدرس یک **قرارداد** ثبت می‌کنه (نرخ هر ساعت/جلسه تدریس، سقف ساعت ماهانه).
2. **آخر هر ماه:** سیستم به‌صورت خودکار، بر اساس **حضور و غیاب واقعی معلم و جلساتی که واقعاً تدریس کرده** (از ماژول Attendance فاز ۳)، مبلغ رو محاسبه می‌کنه و یک **پیش‌نویس فیش حقوقی** می‌سازه.
3. **بازبینی نهایی:** مدیر می‌تونه قبل از صدور قطعی، عدد نهایی هر فیش رو **دستی ویرایش** کنه (تسویه، کسورات موردی، اشتباهات ثبت حضور و ...).
4. **صدور نهایی:** با یک دکمه، فیش‌ها قطعی و «آماده پرداخت» می‌شن — ولی **پرداخت واقعی خارج از اپلیکیشن** انجام می‌شه (دقیقاً مثل چیزی که در فاز ۶ برای «ثبت تسویه بانکی» داشتیم).
5. **خروجی:** اکسل کامل ماه، PDF گروهی/خلاصه، و PDF فیش حقوقی تکی هر نفر.

---

## ۲. مدل داده

### ۲.۱ قرارداد مدرس

```prisma
model TeacherContract {
  id                String   @id @default(uuid())
  tenantId          String
  teacherId         String
  academicYearId    String
  rateType          PayRateType     // HOURLY | PER_SESSION
  rateAmount        Decimal         // نرخ هر ساعت یا هر جلسه
  monthlyHourCap    Int?            // سقف ساعت ماهانه (اختیاری، برای هشدار/محدودیت)
  baseSalary        Decimal?        // حقوق پایه ثابت ماهانه (اگه علاوه بر حق‌التدریس داره)
  effectiveFrom     DateTime
  effectiveTo       DateTime?
  createdBy         String
  createdAt         DateTime @default(now())

  @@index([tenantId, teacherId, academicYearId])
}

enum PayRateType {
  HOURLY
  PER_SESSION
}
```

**نکته:** یک مدرس ممکنه در طول سال قرارداد جدیدی بگیره (افزایش نرخ) — به‌جای ویرایش رکورد قبلی، یک `TeacherContract` جدید با `effectiveFrom` جدید ثبت می‌شه و قبلی با `effectiveTo` بسته می‌شه. این‌جوری تاریخچه نرخ‌ها حفظ می‌شه و محاسبه هر ماه از روی نرخ **همون ماه** انجام می‌شه، نه نرخ فعلی.

### ۲.۲ فیش حقوقی — گسترش با وضعیت Draft/Final

```prisma
enum PayslipStatus {
  DRAFT       // محاسبه خودکار اولیه، هنوز نهایی نشده
  REVIEWED    // مدیر دیده/ویرایش کرده، هنوز صادر نشده
  ISSUED      // نهایی و آماده پرداخت
  SETTLED     // تسویه بانکی ثبت شده (از فاز ۶ موجوده)
}

model Payslip {
  // ... فیلدهای موجود فاز ۶ (teacherId, month, baseSalary, deductions, ...)
  status              PayslipStatus @default(DRAFT)
  calculatedAmount    Decimal       // عدد خام محاسبه‌شده توسط سیستم (تغییرناپذیر — برای audit)
  finalAmount         Decimal       // عددی که واقعاً پرداخت می‌شه (قابل‌ویرایش توسط مدیر)
  editedBy            String?
  editedAt            DateTime?
  editReason          String?       // چرا عدد تغییر کرد — برای audit و شفافیت

  sourceSessionCount  Int           // تعداد جلسات محاسبه‌شده (برای شفافیت/اعتراض بعدی)
  sourceHours         Decimal?
}
```

**نکته حیاتی:** `calculatedAmount` هیچ‌وقت تغییر نمی‌کنه — همیشه نشون می‌ده «سیستم چی حساب کرده». `finalAmount` همونیه که مدیر می‌تونه ویرایش کنه. این جدایی یعنی همیشه می‌شه فهمید آیا و چقدر مدیر از عدد خودکار سیستم فاصله گرفته — هم برای شفافیت داخلی، هم اگه بعداً یک مدرس به مبلغش اعتراض کرد.

---

## ۳. منطق محاسبه خودکار

### ۳.۱ منبع داده
از `TeacherAttendance` (فاز ۳) — رکوردهایی که معلم واقعاً در یک جلسه/زنگ کلاسی «حاضر» بوده، در بازه یک ماه شمسی خاص.

### ۳.۲ فرمول
```
برای هر TeacherContract فعال در اون ماه:
  تعداد جلسات حاضر شده در ماه = COUNT(TeacherAttendance WHERE status = PRESENT AND month = X)

  اگه rateType == PER_SESSION:
     calculatedAmount = تعداد جلسات × rateAmount + (baseSalary یا ۰)

  اگه rateType == HOURLY:
     ساعت واقعی = تعداد جلسات × مدت هر جلسه (از ClassSchedule)
     calculatedAmount = ساعت واقعی × rateAmount + (baseSalary یا ۰)

  کسورات (بیمه/مالیات از فاز ۶ موجوده) از روی همین calculatedAmount کسر می‌شه
  finalAmount = calculatedAmount (پیش‌فرض، تا وقتی مدیر دستی تغییرش بده)
```

### ۳.۳ Job ماهانه
یک `@Cron` (هم‌راستا با تصمیم اخیر برای یادآوری چک — همون `@nestjs/schedule`، نه BullMQ، چون این هم یک Job زمان‌بندی‌شده ساده‌ست نه صف‌محور) که در روز مشخصی از ماه (مثلاً روز آخر ماه شمسی) برای همه مدرسین فعال، `Payslip` با وضعیت `DRAFT` می‌سازه.

---

## ۴. جریان کاری کامل (User Flow)

```
[اول سال تحصیلی]
مدیر → «قراردادهای مدرسین» → برای هر مدرس: نرخ (ساعتی/جلسه‌ای) + سقف ماهانه + حقوق پایه (اختیاری)

[آخر هر ماه — خودکار]
Cron Job اجرا می‌شه → برای هر مدرس یک Payslip با status=DRAFT ساخته می‌شه

[بازبینی مدیر]
مدیر → «حقوق و دستمزد» → جدول ماه جاری (همه مدرسین، وضعیت DRAFT)
  → برای هر ردیف: تعداد جلسات محاسبه‌شده، calculatedAmount، و یک فیلد قابل‌ویرایش finalAmount
  → اگه مدیر عدد رو تغییر بده → باید یک دلیل کوتاه بنویسه (editReason) → status → REVIEWED
  → مدیر جدول رو تک‌تک چک می‌کنه (طبق خواسته خودت: «بتونه تک‌تک چک کنه»)

[صدور نهایی]
مدیر روی دکمه «صدور فیش‌های حقوقی این ماه» می‌زنه
  → همه Payslip های این ماه (Draft/Reviewed) → status = ISSUED
  → این یک عملیات دسته‌ای غیرقابل‌بازگشت‌مانند است (نه Undoable — چون معادل «قطعی کردن حقوق این ماه»ه)
  → بعد از این، پرداخت واقعی خارج از اپ انجام می‌شه، و طبق فاز ۶، تسویه بانکی جداگانه (status → SETTLED) ثبت می‌شه.

[خروجی‌ها — قبل یا بعد از صدور قابل تولید]
  → اکسل کامل ماه (همه مدرسین، همه ستون‌ها)
  → PDF گروهی/خلاصه (یک صفحه در هر مدرس، یا جدول خلاصه)
  → PDF فیش حقوقی تکی (برای ارسال/چاپ مجزا به هر مدرس)
```

---

## ۵. خروجی‌ها — جزئیات فنی

| فرمت | محتوا | ابزار پیشنهادی |
|---|---|---|
| **Excel کامل ماه** | ردیف به ازای هر مدرس: نام، تعداد جلسات، ساعت، calculatedAmount، finalAmount، کسورات، وضعیت | SheetJS (سمت سرور، طبق همون اصل «صحت مالی سمت سرور» که برای Import هم گفتیم) |
| **PDF گروهی/خلاصه** | جدول خلاصه همه مدرسین در یک سند، برای بایگانی/ارائه به مدیر مالی | ابزار PDF بک‌اند (مثلاً `pdf-lib` یا `puppeteer` برای رندر HTML→PDF) |
| **PDF فیش حقوقی تکی** | سربرگ مدرسه + نام مدرس + جزئیات کامل محاسبه (جلسات، نرخ، کسورات، مبلغ نهایی) — قابل چاپ/ارسال مجزا | همون ابزار، تمپلیت جدا |

---

## ۶. نکات امنیتی و صحت (هم‌راستا با اصول قبلی پروژه)

1. **Audit Log کامل برای هر ویرایش `finalAmount`** — چه کسی، از چه عددی به چه عددی، با چه دلیل. این حساس‌ترین نقطه این ماژوله چون مستقیماً روی حقوق واقعی افراد اثر می‌ذاره.
2. **Permission جدا برای صدور نهایی:** پیشنهاد می‌کنم `payroll.finalize` جدا از `payroll.write` باشه — یک حسابدار معمولی بتونه فیش‌ها رو آماده/بازبینی کنه (`payroll.write`)، ولی فقط مدیر ارشد بتونه دکمه «صدور نهایی» رو بزنه (`payroll.finalize`) — دقیقاً همون منطق least-privilege که برای `finance.check.manage` و `finance.fee.import` هم پیاده کردیم.
3. **قفل شدن بعد از صدور:** بعد از `ISSUED`، فیش دیگه قابل‌ویرایش نباشه از مسیر عادی — اگه اصلاح لازم بود، باید یک رکورد اصلاحی جدید (`ADJUSTMENT`) ساخته بشه، نه ویرایش مستقیم رکورد قطعی‌شده. این جلوی تغییرات بی‌ردپا بعد از «قطعی شدن» رو می‌گیره.
4. **هشدار سقف ساعت ماهانه:** اگه `monthlyHourCap` تعریف شده و جلسات واقعی از سقف بیشتر شد، یک Warning (نه بلاک) نشون بده — «این مدرس {X} ساعت بیشتر از سقف قراردادش تدریس کرده» — تا مدیر تصمیم بگیره.

---

## ۷. سوالات باقی‌مانده

- آیا خود **مدرس** باید بتونه فیش حقوقی خودش (بعد از صدور نهایی) رو در پنل خودش ببینه/دانلود کنه، یا این فقط از طریق مدیر (ارسال دستی PDF) در اختیارش قرار می‌گیره؟
- برای «سقف ساعت ماهانه» — اگه یک مدرس بیشتر از سقف تدریس کرد، مبلغ اضافه با همون نرخ عادی حساب بشه، یا نرخ اضافه‌کاری متفاوتی (بیشتر) داره؟