# بریف تکنیکال — سیستم Toast و الگوی «برگشت‌پذیری تلگرامی» (Undoable Actions)

## ۱. فلسفه طراحی

هدف: کاربر (به‌خصوص مدیر مدرسه که روزانه ده‌ها اکشن انجام می‌ده) هیچ‌وقت نباید:
- بدون بازخورد بمونه (آیا عملیات انجام شد؟)
- برای هر اکشن حساس، یک مودال تأیید («آیا مطمئنید؟») ببینه — چون این خودش کندکننده و آزاردهنده‌ست وقتی تعداد اکشن‌ها زیاده.

راه‌حل، الگوی **تلگرام**: به‌جای «تأیید قبل از انجام»، عملیات **بلافاصله (خوش‌بینانه) انجام/نمایش داده می‌شه**، و یک Toast با دکمه «بازگردانی» و شمارش‌معکوس ۵ ثانیه‌ای نشون داده می‌شه. اگه کاربر ظرف ۵ ثانیه کلیک نکنه، عملیات قطعی می‌شه. این هم سرعت عمل رو حفظ می‌کنه، هم مسیر جبران خطا رو می‌ذاره.

---

## ۲. طبقه‌بندی کامل انواع Toast

| نوع | رنگ/آیکون | Duration | کاربرد |
|---|---|---|---|
| **Success** | سبز ✓ | ۳ ثانیه | تکمیل موفق یک عملیات غیرقابل‌بازگشت یا بدون نیاز به Undo (مثلاً «رسید صادر شد») |
| **Error** | قرمز ✕ | ۴-۵ ثانیه (یا تا کلیک) | شکست عملیات؛ همیشه با پیام خطای قابل‌فهم، نه کد خطای خام |
| **Warning** | زرد ⚠ | ۴ ثانیه | هشدار غیربلاک‌کننده (مثلاً «ظرفیت کلاس تقریباً پره») |
| **Info** | آبی ℹ | ۳ ثانیه | اطلاع‌رسانی خنثی (مثلاً «۱۲ پیام جدید») |
| **Loading/Promise** | اسپینر → success/error | تا resolve شدن | برای عملیات async که طول می‌کشه (آپلود فایل، صدور کارنامه گروهی) |
| **Undoable (تلگرامی)** | خنثی + نوار پیشرفت شمارش‌معکوس | دقیقاً ۵ ثانیه | حذف، لغو، تغییرات قابل‌بازگشت — شرح کامل در بخش ۳ |

---

## ۳. الگوی Undo — جزئیات کامل

### ۳.۱ رفتار بصری (مثل تلگرام)

```
┌──────────────────────────────────────────┐
│  🗑  «ناظم پایه دهم» حذف شد        [بازگردانی] │
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░  │  ← نوار پیشرفت که در ۵ ثانیه خالی می‌شه
└──────────────────────────────────────────┘
```
- نوار پیشرفت (progress bar) پایین توست، از پر به خالی در ۵ ثانیه — دقیقاً حس تلگرام رو منتقل می‌کنه (کاربر می‌دونه چقدر وقت داره).
- دکمه «بازگردانی» تا لحظه آخر فعاله.
- هاور کردن ماوس روی توست، شمارش‌معکوس رو **متوقف** می‌کنه (Pause on Hover) — دقیقاً مثل تلگرام، تا اگه کاربر داره می‌خونه، زمانش از دست نره.
- اگه کاربر روی «بازگردانی» بزنه: توست فوراً به یک توست «بازگردانده شد ↩」با duration کوتاه (۲ ثانیه) تغییر می‌کنه.

### ۳.۲ رفتار فنی — دو مدل ممکن، و انتخاب پیشنهادی

**مدل A — تأخیر کامل سمت کلاینت (پیشنهاد برای v1):**
1. کاربر روی «حذف» می‌زنه.
2. UI **فوراً و خوش‌بینانه (Optimistic)** آیتم رو از لیست حذف می‌کنه (از کش TanStack Query).
3. یک تایمر ۵ ثانیه‌ای سمت کلاینت شروع می‌شه؛ درخواست واقعی DELETE به سرور **هنوز ارسال نشده**.
4. اگه ۵ ثانیه بدون کلیک «بازگردانی» بگذره → درخواست واقعی به API ارسال می‌شه.
5. اگه کاربر «بازگردانی» بزنه → تایمر cancel می‌شه، آیتم به کش/لیست برمی‌گرده، هیچ درخواستی به سرور نمی‌ره.

**مزیت:** ساده، هیچ تغییری در بک‌اند لازم نیست.
**محدودیت شناخته‌شده و پذیرفته‌شده:** اگه کاربر تب رو ظرف همون ۵ ثانیه ببنده، عملیات اصلاً به سرور نمی‌رسه — یعنی **چیزی حذف نمی‌شه** (چون درخواست اصلاً fire نشده). این رفتار «fail-safe» است (پیش‌فرض امن: هیچ‌کاری انجام نشد)، نه «fail-danger» — پس قابل‌قبوله و نیازی به راه‌حل پیچیده‌تر (مثل صف سمت سرور) در v1 نیست.

**مدل B — Soft-delete سمت سرور + Grace Window (برای فاز‌های بعدی/اکشن‌های حساس‌تر):**
اگه یک اکشن آنقدر حساسه که حتی «گم‌شدن در بستن تب» هم قابل‌قبول نیست (مثلاً حذف یک قرارداد شهریه)، به‌جای تأخیر کلاینتی، سرور بلافاصله رکورد رو `status: PENDING_DELETE` می‌کنه و یک Job با تأخیر ۵ ثانیه (BullMQ، که از فاز ۵ داریم) واقعاً حذفش می‌کنه؛ Undo یعنی لغو همون Job. این پیچیده‌تره و فقط برای اکشن‌های واقعاً حیاتی توصیه می‌شه، نه همه‌چیز.

**نتیجه‌گیری:** v1 با **مدل A** برای اکثر اکشن‌ها؛ مدل B فقط اگه بعداً یک مورد خاص واقعاً نیاز پیدا کرد.

---

## ۴. کدام اکشن‌ها Undo بگیرن، کدوم نه

### ✅ Undoable (مدل A مناسبشونه)
| ماژول | اکشن |
|---|---|
| Role Builder | حذف نقش، حذف Override دسترسی |
| Members | حذف یک کاربر از کلاس (Unenroll) |
| Homework | حذف تکلیف (قبل از تحویل کسی) |
| Calendar | حذف رویداد/اطلاعیه |
| Matters | حذف رکورد انضباطی/تشویقی |
| Learning Materials | حذف جزوه/محتوا |
| Chat | حذف پیام خودم |
| Question Bank | حذف سوال از بانک |
| Polls | بستن زودهنگام نظرسنجی |

### ⛔ Undo نگیرن — باید با مدل دیگه (تأیید صریح یا غیرقابل‌بازگشت ذاتی) مدیریت بشن
| ماژول | اکشن | چرا |
|---|---|---|
| Finance | تأیید نهایی تراکنش پرداخت زرین‌پال | خارج از کنترل ماست؛ به محض verify شدن سمت درگاه، غیرقابل‌بازگشته |
| Finance | صدور فیش حقوقی و تسویه بانکی | سند مالی رسمی؛ باید مودال تأیید صریح داشته باشه، نه toast |
| Auth | حذف کامل حساب کاربری | برگشت‌ناپذیر و پرریسک؛ مودال تأیید + تایپ نام برای تأیید |
| SaaS Admin | Suspend/Delete یک تننت (مدرسه) | تأثیر روی صدها کاربر؛ نیازمند تأیید دوعامله یا حداقل مودال با توضیح پیامد |
| Exams | پایان‌دادن دستی به یک آزمون در حال برگزاری | روی دانش‌آموزان دیگه تأثیر فوری داره؛ مودال تأیید |

**قاعده کلی برای تصمیم‌گیری آینده:** اگه یک اکشن (۱) فقط روی داده‌ی خودِ کاربر انجام‌دهنده اثر می‌ذاره یا به‌راحتی reversible هست، و (۲) به یک سیستم بیرونی (بانک، درگاه پرداخت) وصل نیست → کاندید Undo. اگه غیر از این‌هاست → مودال تأیید صریح.

---

## ۵. معماری فنی پیاده‌سازی (React + Vite + Sonner + TanStack Query)

### ۵.۱ هوک عمومی `useUndoableMutation`

```ts
// lib/hooks/useUndoableMutation.ts
function useUndoableMutation<TData, TVariables>(config: {
  mutationFn: (vars: TVariables) => Promise<TData>;
  optimisticUpdate: (vars: TVariables) => void;   // فوراً از کش/UI حذف می‌کنه
  revertUpdate: (vars: TVariables) => void;       // در صورت Undo، برمی‌گردونه
  undoLabel: string;                              // متن toast، مثلاً «نقش حذف شد»
  delayMs?: number;                                // پیش‌فرض 5000
}) {
  return (vars: TVariables) => {
    optimisticUpdate(vars);

    let cancelled = false;
    const timer = setTimeout(() => {
      if (!cancelled) config.mutationFn(vars);
    }, config.delayMs ?? 5000);

    toast(config.undoLabel, {
      action: {
        label: 'بازگردانی',
        onClick: () => {
          cancelled = true;
          clearTimeout(timer);
          revertUpdate(vars);
          toast.success('بازگردانده شد');
        },
      },
      duration: config.delayMs ?? 5000,
      // نوار پیشرفت با CSS سفارشی sonner یا کامپوننت toast.custom
    });
  };
}
```

هر ماژول (Role Builder، Homework، Matters، ...) این هوک رو با `optimisticUpdate`/`mutationFn`/`revertUpdate` مخصوص خودش صدا می‌زنه — منطق مشترک (تایمر، توست، لغو) یک‌بار نوشته می‌شه.

### ۵.۲ نوار پیشرفت شمارش‌معکوس

Sonner به‌صورت پیش‌فرض progress bar نداره؛ باید با `toast.custom()` یک کامپوننت اختصاصی (`UndoToast.tsx`) ساخته بشه که:
- یک `<div>` با انیمیشن CSS `width: 100% → 0%` در مدت `duration` داره.
- روی `onMouseEnter` انیمیشن `animation-play-state: paused` می‌شه (Pause on Hover) و تایمر JS هم متوقف می‌شه (باید با `useRef` زمان باقی‌مانده رو نگه داره).

### ۵.۳ Positioning و RTL

- موقعیت پیش‌فرض توست‌ها: **پایین‌راست در LTR می‌شه پایین‌چپ در RTL** — Sonner از `dir` صفحه خودش این رو تشخیص می‌ده اگه HTML شما `dir="rtl"` رو درست ست کرده باشه (طبق تنظیمات RTL که برای shadcn/ui گفتیم).
- Icon های جهت‌دار (فلش برگشت روی دکمه Undo) باید mirror بشن — طبق همون قانون RTL کلی frontend brief.

### ۵.۴ Stacking (چند Undo هم‌زمان)

- اگه کاربر پشت‌سرهم چند آیتم حذف کنه (مثلاً چند سوال از بانک سوال)، هر کدوم toast جدا می‌گیرن، پشته می‌شن (Sonner این رو built-in داره: `visibleToasts` رو محدود کن، مثلاً به ۳، بقیه جمع بشن زیر یک «+۲ مورد دیگر»).
- هر toast تایمر **مستقل** خودش رو داره — Undo کردن یکی، بقیه رو لغو نمی‌کنه.

---

## ۶. دسترسی‌پذیری (Accessibility)

- تمام توست‌ها باید در یک `aria-live="polite"` region رندر بشن (Sonner این رو داخلی مدیریت می‌کنه) تا screen reader بلافاصله متن رو بخونه.
- توست‌های Error باید `aria-live="assertive"` باشن (فوری‌تر خونده بشن).
- دکمه «بازگردانی» باید با Tab قابل‌فوکوس باشه و میان‌بر صفحه‌کلید (اختیاری): `Ctrl+Z` بلافاصله بعد از هر اکشن Undoable، آخرین toast فعال رو trigger کنه — این دقیقاً همون رفتاریه که کاربرهای حرفه‌ای (مدیر مدرسه که کلی کار روزانه داره) انتظارش رو دارن.

---

## ۷. جمع‌بندی چک‌لیست پیاده‌سازی

- [ ] نصب `sonner` + اتصال `<Toaster />` در `app/providers.tsx`
- [ ] پیاده‌سازی `UndoToast.tsx` (کامپوننت سفارشی با نوار پیشرفت + pause-on-hover)
- [ ] پیاده‌سازی هوک عمومی `useUndoableMutation`
- [ ] اتصال هوک به لیست اکشن‌های جدول بخش ۴ (یکی‌یکی، ماژول به ماژول)
- [ ] تنظیم `visibleToasts` و موقعیت (`position="bottom-left"` در RTL)
- [ ] تست دستی: بستن تب وسط ۵ ثانیه → مطمئن شو هیچ درخواستی نرفته (چک Network tab)
- [ ] تست a11y با یک screen reader (NVDA/VoiceOver) روی حداقل یک اکشن Undoable
- [ ] مستندسازی برای تیم: «قانون کلی» بخش ۴ رو در `CLAUDE.md` یا `ARCHITECTURE.md` هم اضافه کن تا فیچرهای بعدی هم همین الگو رو دنبال کنن، نه هرکس یک سبک