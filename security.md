# معماری امنیتی سطح سازمانی — پلتفرم رُکاد
### الگوبرداری از عملکرد بزرگ‌ترین محصولات (Stripe, Google Workspace, Notion, GitHub) — بدون محدودیت فنی/بودجه‌ای

## فلسفه پایه: Zero Trust + Defense in Depth

اصل حاکم: **هیچ لایه‌ای به لایه دیگه اعتماد کامل نداره.** حتی اگه یک درخواست از داخل شبکه داخلی بیاد، دوباره احراز هویت و مجوز می‌شه. هر لایه (شبکه، اپلیکیشن، داده) مستقل امن‌سازی می‌شه، طوری که نفوذ به یک لایه، بقیه رو خودکار در معرض خطر نذاره. هدف نهایی: حتی اگه یک سرور یا یک توکن لو بره، **شعاع انفجار (Blast Radius)** محدود بمونه.

---

## ۱. لایه هویت و احراز هویت (Identity & Access)

| مورد | توضیح |
|---|---|
| **MFA اجباری برای نقش‌های حساس** | سوپرادمین و هر نقشی با `finance.write`/`payroll.write`/`rbac.manage` باید 2FA اجباری داشته باشه (TOTP یا WebAuthn) — نه اختیاری. |
| **WebAuthn / Passkey برای سوپرادمین** | به‌جای رمز عبور ساده، ورود سوپرادمین با کلید امنیتی سخت‌افزاری (YubiKey) یا Passkey دستگاه — عملاً phishing-proof. |
| **Risk-Based / Adaptive Authentication** | اگه لاگین از یک IP/کشور/دستگاه جدید بیاد (خصوصاً برای نقش‌های مالی)، یک لایه تأیید اضافه (کد ایمیل/SMS) قبل از تکمیل ورود فعال بشه. |
| **Session Anomaly Detection** | تشخیص «سفر غیرممکن» (Impossible Travel) — مثلاً یک کاربر از تهران لاگین کرده و ۵ دقیقه بعد از یک کشور دیگه — باعث invalidation فوری session و هشدار بشه. |
| **Device Fingerprinting** | ثبت دستگاه‌های شناخته‌شده هر کاربر؛ ورود از دستگاه ناشناس برای نقش‌های حساس یک هشدار ایمیلی («ورود جدید از دستگاه ناشناس») بفرسته. |
| **SSO/SAML برای آینده B2B** | وقتی مدارس بزرگ‌تر یا سازمان‌های آموزشی چندشعبه‌ای مشتری شدن، امکان اتصال IdP خودشون (Google Workspace, Azure AD) — کاهش ریسک مدیریت رمز عبور توسط ما. |
| **Least Privilege به‌صورت پیش‌فرض** | هر نقش جدید به‌صورت پیش‌فرض هیچ Permission ای نداره؛ باید صریحاً اضافه بشه (Deny by Default، نه Allow by Default). |
| **Access Recertification دوره‌ای** | هر سه ماه، یک گزارش خودکار «چه کسانی چه دسترسی‌هایی دارن» برای مدیر مدرسه/سوپرادمین ارسال بشه تا دسترسی‌های اضافی/فراموش‌شده رو خودشون پاک کنن. |

---

## ۲. لایه شبکه (Network)

| مورد | توضیح |
|---|---|
| **WAF در لبه شبکه** | Cloudflare Enterprise WAF (یا AWS WAF) جلوی تمام ترافیک — فیلتر خودکار الگوهای شناخته‌شده حمله (SQLi، XSS، Path Traversal) قبل از رسیدن به سرور. |
| **DDoS Protection** | از همون لایه Cloudflare/AWS Shield — برای یک SaaS مالی که در دسترس بودنش critical، این ضروریه. |
| **جداسازی شبکه‌ای (Network Segmentation)** | دیتابیس، Redis، MinIO در Private Subnet — هیچ IP عمومی مستقیم ندارن؛ فقط از طریق سرورهای اپلیکیشن (که خودشون در subnet جدا هستن) قابل دسترسین. |
| **mTLS بین سرویس‌ها** | اگه در آینده معماری میکروسرویس‌تر شد (مثلاً سیستم کا به‌عنوان سرویس جدا وصل بشه)، ارتباط بین سرویس‌ها با Certificate متقابل (نه فقط API Key). |
| **دسترسی مدیریتی به دیتابیس فقط از طریق Bastion/VPN** | هیچ دولوپری مستقیم به IP دیتابیس production وصل نمی‌شه؛ فقط از طریق یک Bastion Host با لاگ کامل session. |
| **IP Allowlisting برای پنل سوپرادمین (اختیاری ولی قوی)** | اگه تیم داخلی از IPهای ثابت کار می‌کنه، پنل سوپرادمین می‌تونه فقط از IPهای مجاز در دسترس باشه. |

---

## ۳. لایه اپلیکیشن (Application Security)

| مورد | توضیح |
|---|---|
| **SAST در CI/CD** | هر Pull Request قبل از merge، با ابزار Static Analysis (مثل Semgrep یا SonarQube) اسکن بشه — خطاهای امنیتی رایج (SQLi، XSS، Hardcoded Secret) قبل از merge گیر بیفتن. |
| **Dependency & Vulnerability Scanning** | Snyk یا Dependabot به‌صورت خودکار روی `package.json`/`pnpm-lock.yaml` — هر باگ امنیتی شناخته‌شده در کتابخونه‌های استفاده‌شده فوراً هشدار بده. |
| **Secret Scanning** | ابزار مثل `gitleaks` در CI — جلوی commit شدن تصادفی کلید/رمز رو می‌گیره، حتی قبل از push. |
| **Rate Limiting تطبیقی (Adaptive)** | نه فقط یک عدد ثابت، بلکه بر اساس رفتار — اگه یک IP یا کاربر رفتار غیرعادی نشون بده (مثلاً درخواست‌های خیلی سریع)، محدودیت به‌صورت پویا سخت‌تر بشه (شبیه الگوریتم Stripe). |
| **CAPTCHA/Challenge برای فرم‌های حساس** | ثبت‌نام مدرسه جدید، فراموشی رمز عبور — با Cloudflare Turnstile (جایگزین نامرئی و سریع‌تر از reCAPTCHA). |
| **CSP سخت‌گیرانه با Nonce** | Content-Security-Policy که فقط اسکریپت‌های دارای nonce معتبر رو اجرا می‌کنه — دفاع لایه‌ی دوم در برابر XSS حتی اگه یک‌جا sanitization فراموش بشه. |
| **Security Headers کامل** | Helmet.js با تنظیمات کامل: HSTS، X-Frame-Options، X-Content-Type-Options، Referrer-Policy. |

---

## ۴. لایه داده (Data Security)

| مورد | توضیح |
|---|---|
| **رمزنگاری در حالت سکون (Encryption at Rest)** | PostgreSQL با TDE (Transparent Data Encryption) در سطح دیسک/volume، به‌علاوه رمزنگاری فیلدی اضافه برای داده فوق‌حساس (کد ملی، شبا). |
| **مدیریت کلید با KMS/HSM** | کلیدهای رمزنگاری در یک Key Management Service (AWS KMS، HashiCorp Vault، یا حتی یک HSM اختصاصی) — نه در کد یا `.env` ساده. چرخش خودکار کلید (Key Rotation) هر ۹۰ روز. |
| **بکاپ رمزنگاری‌شده + تست بازیابی دوره‌ای** | بکاپ‌ها باید رمزنگاری‌شده ذخیره بشن، و **هر ماه یک بازیابی آزمایشی واقعی** انجام بشه (نه فقط اعتماد به این‌که بکاپ می‌گیریم) — این دقیقاً همون چیزیه که خیلی از شرکت‌ها در بحران واقعی یاد می‌گیرن: بکاپی که تست نشده، بکاپ نیست. |
| **طبقه‌بندی داده (Data Classification)** | هر جدول/فیلد برچسب بخوره: عمومی / داخلی / محرمانه / فوق‌محرمانه (کد ملی، شبا) — سیاست دسترسی و رمزنگاری بر این اساس تنظیم بشه. |
| **سیاست نگه‌داری و حذف داده (Retention & Deletion Policy)** | مشخص بشه داده دانش‌آموزانی که مدرسه رو ترک کردن، بعد از چند سال چه سرنوشتی داره (آرشیو رمزنگاری‌شده، یا حذف کامل با رعایت الزامات آموزشی/قانونی). |
| **Immutable Audit Log جدا از دیتابیس اصلی** | لاگ ممیزی به یک سیستم append-only جدا (مثلاً یک S3 Bucket با Object Lock، یا سرویس لاگ جدا) هم نوشته بشه — تا حتی اگه دیتابیس اصلی compromise بشه، تاریخچه تغییرات دست‌نخورده بمونه. |

---

## ۵. زیرساخت و DevSecOps

| مورد | توضیح |
|---|---|
| **اسکن Image کانتینر** | قبل از هر deploy، Docker image ها با Trivy یا Snyk Container اسکن بشن (آسیب‌پذیری OS-level). |
| **Infrastructure as Code + اسکن امنیتی IaC** | اگه Terraform/Pulumi استفاده می‌شه، با ابزار Checkov یا tfsec اسکن بشه قبل از apply. |
| **Immutable Infrastructure** | سرورها به‌جای patch دستی، با هر تغییر از نو build و deploy بشن — کاهش drift و backdoor احتمالی. |
| **IAM با کمترین دسترسی ممکن** | هر سرویس (اپلیکیشن، CI/CD، مانیتورینگ) دقیقاً همون دسترسی cloud که لازم داره رو بگیره، نه بیشتر. |
| **Secrets Manager به‌جای `.env` در production** | HashiCorp Vault یا AWS Secrets Manager — secretها runtime تزریق بشن، نه در فایل استاتیک. |

---

## ۶. نظارت، تشخیص و واکنش (Monitoring, Detection & Response)

| مورد | توضیح |
|---|---|
| **SIEM متمرکز** | جمع‌آوری همه لاگ‌ها (اپلیکیشن، شبکه، دیتابیس) در یک ابزار (Wazuh، Datadog Security Monitoring، یا Elastic Security) برای همبستگی رویدادها و تشخیص الگوهای مشکوک. |
| **هشدار Real-time برای رویدادهای حساس** | مثال‌ها: تغییر گروهی نقش/دسترسی، export حجیم داده، تعداد زیاد تراکنش ناموفق پرداخت در بازه کوتاه، ورود سوپرادمین از IP جدید — همه این‌ها فوری (Slack/PagerDuty/SMS) به تیم امنیتی اطلاع داده بشن. |
| **تشخیص تقلب مالی** | برای ماژول شهریه/پرداخت: الگوریتم ساده Rule-based (مثلاً چند تراکنش ناموفق پشت‌سرهم با مبالغ متفاوت از یک حساب) که می‌تونه بعداً به یک مدل یادگیری ماشین ارتقا پیدا کنه. |
| **Uptime & Synthetic Monitoring** | چک دوره‌ای خودکار از بیرون (نه فقط health check داخلی) که مسیرهای حیاتی (لاگین، پرداخت) واقعاً کار می‌کنن. |

---

## ۷. فرآیند و سازمانی (Process — جایی که بزرگ‌ترین محصولات واقعاً فرق می‌کنن)

اینجا نکته کلیدیه: تفاوت اصلی محصولات بزرگ با بقیه، فقط ابزار نیست — **فرآیندیه**:

| مورد | توضیح |
|---|---|
| **Threat Modeling برای هر فیچر حساس جدید** | قبل از پیاده‌سازی هر ماژول حساس (مثلاً همین Role Creator یا هر چیز مالی)، یک جلسه کوتاه «چطور می‌شه از این سوءاستفاده کرد؟» — دقیقاً چیزی که در طول همین گفتگو به‌صورت ad-hoc انجام دادیم (idempotency پرداخت، ضدتقلب آزمون)، باید رسمی و مستند بشه. |
| **Security Review Gate قبل از Merge** | ماژول‌های حساس (RBAC، Finance، Auth) نیاز به یک review اختصاصی امنیتی دارن، جدا از code review عادی. |
| **تست نفوذ دوره‌ای (Penetration Testing)** | هر ۳-۶ ماه، یک تیم خارجی مستقل روی کل پلتفرم پن‌تست انجام بده — نه فقط تکیه به تیم داخلی. |
| **برنامه Bug Bounty** | حتی یک برنامه کوچیک (با پلتفرمی مثل HackerOne یا حتی یک صفحه `security.txt` با آدرس تماس) باعث می‌شه محقق‌های امنیتی مستقل، آسیب‌پذیری‌ها رو به‌جای فروش در بازار سیاه، به شما گزارش بدن. |
| **Incident Response Plan مکتوب** | یک سند مشخص: «اگه دیتابیس لو رفت، این ۱۰ قدم دقیق رو انجام بده» — شامل: قطع دسترسی، اطلاع‌رسانی به مدارس تحت تأثیر، چرخش همه کلیدها، تحلیل ریشه‌ای. این سند باید **قبل از وقوع حادثه** نوشته بشه، نه وسط بحران. |
| **RPO/RTO مشخص برای بازیابی فاجعه** | مشخص کن اگه کل زیرساخت از دست رفت، چقدر داده حاضری از دست بدی (Recovery Point Objective) و چقدر طول می‌کشه برگردی سرپا (Recovery Time Objective) — و معماری بکاپ رو بر این اساس طراحی کن. |
| **آموزش امنیتی مداوم تیم** | حتی بهترین معماری هم با یک دولوپر که روی یک لینک فیشینگ کلیک کنه، دور زده می‌شه — آموزش دوره‌ای (Phishing Simulation) برای کل تیم. |

---

## ۸. نکته‌ی خاص برای رُکاد — چون داده کودکان دخیله

چون کاربران این پلتفرم شامل **دانش‌آموزان (که اکثراً زیر ۱۸ سالن)** می‌شن، سطح محافظت از داده‌شون باید بالاتر از یک SaaS عادی باشه، حتی فراتر از چیزی که برای کاربر بزرگسال معمول جواب می‌ده:
- دسترسی به پروفایل دانش‌آموز باید محدودتر از دسترسی به پروفایل معلم/پرسنل باشه (حتی داخل خود مدرسه).
- هر دسترسی به داده دانش‌آموز (نه فقط تغییر، بلکه حتی مشاهده گزارش‌های حساس مثل پرونده انضباطی) باید Audit Log داشته باشه.
- عکس/ویدیوی دانش‌آموزان (اگه در آینده در Media Feed یا محتوای آموزشی باشه) باید کنترل دسترسی سخت‌گیرانه‌تری نسبت به محتوای عادی داشته باشه.

---

## جمع‌بندی: نقشه راه (چون حتی بزرگ‌ترین شرکت‌ها هم همه‌چیز رو روز اول نمی‌سازن)

با این‌که گفتی محدودیت فنی نداریم، بعضی از این موارد (Threat Modeling فرهنگی، Incident Response Plan، Access Recertification دوره‌ای) **زمان و انضباط سازمانی** می‌خوان، نه فقط بودجه. پیشنهاد توالی واقع‌بینانه:

```
موج اول (فوری):      MFA برای نقش‌های حساس + Secrets Manager + Rate Limiting + WAF
موج دوم (۱-۲ ماه):    SIEM + Encryption at Rest + Audit Log مجزا + CI/CD Security Scanning
موج سوم (۳-۶ ماه):    Penetration Testing اول + Incident Response Plan + Access Recertification
موج چهارم (مداوم):    Bug Bounty + Threat Modeling فرهنگ‌شده + آموزش تیم
```

می‌خوای روی موج اول (که فوری‌ترینه) یک پلن پیاده‌سازی دقیق‌تر بنویسم — مثلاً انتخاب دقیق ابزار MFA برای NestJS، یا نحوه راه‌اندازی Vault؟