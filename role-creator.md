# بریف — سازنده نقش و مدیریت دسترسی (Role Creator & Access Manager)
### محل: پنل مدیر مدرسه (School Admin Panel) — ماژول `school-admin/role-builder`

## ۱. هدف

مدیر مدرسه بدون نیاز به دخالت تیم فنی، بتونه:
1. لیست تمام قابلیت‌های سیستم (Permission ها) رو به **زبان ساده و فارسی** ببینه — نه به شکل کلیدهای فنی مثل `grades.write`.
2. نقش‌های سفارشی مدرسه بسازه (مثل «ناظم پایه دهم»، «مشاور تحصیلی»، «حسابدار شعبه») و برای هرکدوم مجموعه‌ای از دسترسی‌ها انتخاب کنه.
3. لیست همه اعضای مدرسه (پرسنل، معلم، مدیر، مشاور...) رو ببینه و نقششون رو عوض کنه، یا مستقل از نقش، یک دسترسی خاص رو برای یک نفر باز/بسته کنه (Override).

این ماژول مستقیماً روی زیرساخت RBAC فاز ۲ بک‌اند (`SchoolRole`, `Permission`, مدل هیبریدی enum+Permission) سوار می‌شه — منطق سمت سرور از قبل وجود داره، اینجا فقط رابط کاربری و چند endpoint تکمیلی لازمه.

---

## ۲. دسترسی به این صفحه

- فقط `SCHOOL_ADMIN` (یا هر نقشی که Permission ویژه `rbac.manage` رو داشته باشه — خود این صفحه هم باید پشت یک Permission باشه، نه فقط enum درشت، چون ممکنه بخوایم بعداً به یک معاون هم دسترسی محدود بدیم).
- مدیر مدرسه **نمی‌تونه** نقش/دسترسی خودش رو به شکلی تغییر بده که از پنل بیرون بیفته (قانون محافظتی — بخش ۶).

---

## ۳. سه صفحه اصلی

### الف) کاتالوگ دسترسی‌ها (Permission Catalog) — فقط نمایشی

جدول/لیستی از تمام Permission های سیستم (۲۷ مورد فعلی که در فاز ۲ ثبت شدن)، **گروه‌بندی‌شده بر اساس حوزه**:
- 🏫 آموزشی (ERP): مدیریت کلاس، دانش‌آموز، معلم
- 📝 سنجش (LMS): آزمون، نمره، بانک سوال
- 📋 عملیات روزانه (Operations): حضور و غیاب، تکلیف، تقویم
- 💰 مالی (Finance): شهریه، حقوق، گزارش مالی

هر Permission با:
- **عنوان ساده فارسی** (نه کلید فنی) — مثلاً به‌جای `grades.write` بنویسه: «ثبت و ویرایش نمرات»
- **توضیح یک‌خطی** — «این دسترسی به کاربر اجازه می‌ده نمرات دانش‌آموزان رو ثبت یا اصلاح کنه»
- آیکون مرتبط با حوزه

> این صفحه صرفاً مرجع/دیکشنری است؛ ورودی کاربر ندارد.

### ب) سازنده نقش (Role Builder)

- **لیست نقش‌های موجود مدرسه** (پیش‌فرض‌ها مثل معلم/ناظم + نقش‌های سفارشی که مدیر ساخته).
- دکمه «ساخت نقش جدید»:
  1. نام نقش (فارسی، آزاد) — مثلاً «مشاور پایه یازدهم»
  2. توضیح اختیاری
  3. **چک‌باکس‌های Permission** به همون شکل گروه‌بندی‌شده کاتالوگ (بخش الف) — نه لیست تخت، بلکه آکاردئون باز/بسته بر اساس حوزه
  4. پیش‌نمایش «این نقش می‌تونه: [خلاصه زبان طبیعی از پرمیشن‌های انتخاب‌شده]»
- **ویرایش نقش موجود:** همون فرم، از قبل پر شده.
- **حذف نقش:** فقط اگر هیچ کاربری الان اون نقش رو نداشته باشه؛ در غیر این صورت پیام: «این نقش به X نفر اختصاص داده شده — اول نقش اون‌ها رو تغییر بده».

### ج) اعضا و دسترسی‌ها (Members & Access)

- **جدول تمام کاربران مدرسه** (با جستجو و فیلتر بر اساس نوع: معلم/پرسنل/مدیر...، و بر اساس نقش فعلی).
- هر ردیف:
  - نام، نوع پایه (`User.role` — معلم/پرسنل/...، این تغییرناپذیره از این صفحه)
  - **نقش(های) اختصاصی فعلی** (چون یک نفر می‌تونه چند نقش داشته باشه — مثلاً هم دبیر هم ناظم)
  - دکمه «تغییر نقش» → چند‌انتخابی از نقش‌های موجود مدرسه
  - دکمه «دسترسی‌های اختصاصی» → یک Override مستقل از نقش: باز یا بسته کردن دستی یک Permission خاص برای همون فرد (مثلاً یک معلم که به‌طور موردی دسترسی گزارش مالی هم می‌خواد، بدون این‌که نقشش عوض بشه)
- کلیک روی هر کاربر، یک پنل کناری/مودال باز می‌کنه که خلاصه کامل دسترسی‌های مؤثرش رو نشون می‌ده (نقش + override).

---

## ۴. مدل داده (آنچه از قبل هست + آنچه اضافه می‌شه)

**از قبل در بک‌اند (فاز ۲):**
- `SchoolRole`, `Permission`, جدول واسط `User ↔ SchoolRole`
- `PermissionsGuard` + `@RequirePermissions`

**نیاز جدید برای این فیچر:**
- **متادیتای زبان ساده روی `Permission`:** فیلدهای `labelFa`, `descriptionFa`, `category` (اگه از قبل نیست) — یا یک جدول/فایل config جدا `PermissionMeta` اگه نخوایم اسکیمای اصلی رو دست بزنیم.
- **`UserPermissionOverride`** (جدول جدید): `userId`, `permissionKey`, `effect` (`GRANT` | `REVOKE`), `grantedBy`, `createdAt` — برای دسترسی موردی مستقل از نقش. منطق محاسبه دسترسی مؤثر: `(دسترسی‌های همه نقش‌های کاربر) ∪ GRANT ها − REVOKE ها`.
- **Endpoint لیست همه اعضای تننت با نقش فعلی‌شون** (اگه از `MembersModule` قابل ترکیب نیست، یک endpoint تجمیعی جدید در `RbacModule` بساز: `GET /api/v1/rbac/members`).

---

## ۵. Endpointهای پیشنهادی (بخشی موجود، بخشی جدید)

| متد | مسیر | وضعیت | توضیح |
|---|---|---|---|
| GET | `/api/v1/rbac/permissions` | احتمالاً موجود | کاتالوگ با متادیتای فارسی |
| GET | `/api/v1/rbac/roles` | موجود | لیست نقش‌های تننت |
| POST | `/api/v1/rbac/roles` | موجود | ساخت نقش |
| PATCH | `/api/v1/rbac/roles/:id` | موجود | ویرایش نقش |
| DELETE | `/api/v1/rbac/roles/:id` | بررسی شود | باید چک «آیا کسی این نقش رو داره» انجام بده |
| GET | `/api/v1/rbac/members` | **جدید** | لیست کاربران تننت + نقش‌های فعلی، برای صفحه ج |
| PATCH | `/api/v1/rbac/members/:userId/roles` | احتمالاً موجود در قالب دیگه | تخصیص/حذف نقش از کاربر |
| PATCH | `/api/v1/rbac/members/:userId/overrides` | **جدید** | GRANT/REVOKE دستی یک Permission |

---

## ۶. قوانین محافظتی (Guardrails)

1. **مدیر مدرسه نمی‌تونه دسترسی مدیریت نقش (`rbac.manage`) رو از خودش بگیره** — جلوی قفل‌شدن کامل پنل رو می‌گیره.
2. **همیشه باید حداقل یک کاربر با دسترسی کامل مدیریتی در تننت باقی بمونه** — چک سمت سرور قبل از هر تغییر نقش/Override.
3. **هر تغییر نقش/Override باید در `AuditLog`** (که از فاز ۱ داریم) با جزئیات (چه کسی، برای کی، چه تغییری) ثبت بشه — چون این صفحه مستقیماً امنیت دسترسی رو تغییر می‌ده.
4. **Permission های حساس (مثل `finance.write`, `payroll.write`) در UI با یک نشان هشدار (⚠️) متمایز بشن** تا مدیر تصادفی به کسی ندش.

---

## ۷. جریان کاربری خلاصه (User Flow)

```
مدیر وارد Role Builder می‌شه
  → کاتالوگ رو مرور می‌کنه (اختیاری، برای آشنایی)
  → یک نقش جدید می‌سازه با چند Permission تیک‌خورده
  → می‌ره به تب «اعضا»
  → یک معلم رو پیدا می‌کنه (جستجو)
  → نقش «ناظم پایه دهم» رو بهش اضافه می‌کنه (علاوه بر نقش معلم)
  → یک Permission موردی («مشاهده گزارش مالی») رو هم جدا براش باز می‌کنه
  → تغییرات فوراً اعمال و در AuditLog ثبت می‌شه
```

---

## ۸. پیشنهاد کامپوننت‌بندی فرانت (React + Vite طبق استک تعیین‌شده)

```
modules/school-admin/role-builder/
├── pages/
│   ├── PermissionCatalogPage.tsx
│   ├── RolesListPage.tsx
│   ├── RoleFormPage.tsx          (ساخت/ویرایش)
│   └── MembersAccessPage.tsx
├── components/
│   ├── PermissionCategoryAccordion.tsx
│   ├── PermissionCheckboxGroup.tsx
│   ├── RolePreviewSummary.tsx     (خلاصه زبان طبیعی نقش)
│   ├── MemberAccessDrawer.tsx     (پنل کناری خلاصه دسترسی مؤثر)
│   └── OverrideToggle.tsx
├── hooks/
│   ├── usePermissionCatalog.ts    (TanStack Query)
│   ├── useSchoolRoles.ts
│   └── useMemberAccess.ts
└── api/
    └── rbac.api.ts
```