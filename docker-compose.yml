services:
  postgres:
    image: postgres:16-alpine
    container_name: rokad_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-rokad_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rokad_secret_2026}
      POSTGRES_DB: ${POSTGRES_DB:-rokad_db}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - rokad_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-rokad_user} -d ${POSTGRES_DB:-rokad_db}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: rokad_redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-rokad_redis_secret}
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - rokad_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-rokad_redis_secret}", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:RELEASE.2024-05-10T01-41-38Z
    container_name: rokad_minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-rokad_minio_admin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-rokad_minio_secret_2026}
    ports:
      - "${MINIO_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - rokad_minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  rokad_postgres_data:
    driver: local
  rokad_redis_data:
    driver: local
  rokad_minio_data:
    driver: local
