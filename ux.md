# منشور جامع تجربه کاربری (UX) و استانداردهای رابط کاربری (UI) پلتفرم رُکاد

> **نسخه:** ۲.۰ (بازنویسی کامل و جامع)  
> **دامنه شمول:** کلیه ماژول‌ها، صفحات، کامپوننت‌ها و جریان‌های کاری فرانت‌اند پلتفرم رُکاد (`frontend/src/`)  
> **اسناد بالادستی و مکمل:** [CLAUDE.md](file:///Users/behrad/Desktop/Projects/rokad-platform/CLAUDE.md) (معماری و مهندسی)، [jalalidate.md](file:///Users/behrad/Desktop/Projects/rokad-platform/jalalidate.md) (معماری تقویم شمسی)

---

## مقدمه و بیانیه چشم‌انداز UX

پلتفرم رُکاد یک سیستم یکپارچه مدیریت مدارس و هنرستان‌ها است که طیف متنوعی از کاربران ایرانی — از معلمان داخل کارگاه و کلاس، تا دانش‌آموزان روی گوشی‌های اقتصادی، والدین در حال تردد و مدیران پشت میز کار — روزانه با آن تعامل دارند.

> [!CRITICAL]
> **اصل بنیادین رُکاد:**  
> **«سادگی اعتیادآور، سرعت بالا، بدون نیاز به آموزش، و با محوریت قطعی موبایل و زبان فارسی.»**  
> هیچ صفحه یا کامپوننتی نباید ابتدا برای دسکتاپ طراحی یا پیاده‌سازی شده و سپس با تقلیل به موبایل برسد. نقطه مبنای ۱۰۰٪ اجزای پلتفرم، صفحه نمایش ۳۶۰ پیکسلی لمسی موبایل است.

---

## ۱. اصل حاکمیتی Mobile-First و معماری واکنش‌گرا

### ۱.۱ چرا Mobile-First یک الزام زیستی است؟
۱. **معلم سر کلاس:** دبیران و هنرآموزان در حین ایستادن، تدریس یا بازرسی کارگاه، گوشی را با یک دست در دست دارند. هر فرایندی که به دو دست، دقت ماوس یا زوم صفحه نیاز داشته باشد، در عمل شکست می‌خورد.
۲. **والدین و دانش‌آموزان:** بیش از ۸۵٪ دسترسی اولیاء و دانش‌آموزان از طریق تلفن‌های هوشمند همراه و روی شبکه‌های اینترنت ۳G/۴G صورت می‌پذیرد.
۳. **تنوع سخت‌افزاری:** کف دستگاه‌های بازار آموزشی ایران، نمایشگرهای ۳۶۰ پیکسلی اندرویدی است؛ اگر روی ۳۶۰px عالی کار نکند، در سراسر کشور لنگ می‌زند.

### ۱.۲ Breakpointهای استاندارد پروژه
در سیستم استایل‌دهی (Tailwind CSS)، استایل‌های پیش‌فرض بدون پیشوند برای **موبایل** تعریف می‌شوند و با `min-width` برای نمایشگرهای بزرگ‌تر گسترش می‌یابند:

| کلاس Breakpoint | حداقل عرض | دستگاه هدف | رفتار UI |
|---|---|---|---|
| **Base (پیش‌فرض)** | `< 640px` | موبایل اقتصادی (`360px` تا `400px`) | تمام‌عرض، کارت‌های عمودی، ناوبری پایینی، دکمه‌های چسبان |
| **`sm:`** | `≥ 640px` | موبایل بزرگ / Phablet | تنظیم پدینگ‌ها، چیدمان دوتایی کارت‌های فشرده |
| **`md:`** | `≥ 768px` | تبلت عمودی / iPad | فعال‌شدن سایدبار جمع‌شونده، فرم‌های دو ستونه |
| **`lg:`** | `≥ 1024px` | لپ‌تاپ و تبلت افقی | جدول داده کامل، سایدبار ثابت، داشبوردهای چند ستونه |
| **`xl:`** | `≥ 1280px` | مانیتور رومیزی اداری | ماتریس‌های گسترده مالی و نمرات، فضاهای تنفس استاندارد |

```css
/* استاندارد صحیح رُکاد: پیش‌فرض موبایل، گسترش برای دسکتاپ */
.action-button {
  width: 100%;
  min-height: 48px;
  font-size: 15px;
}

@media (min-width: 768px) {
  .action-button {
    width: auto;
    min-height: 40px;
    font-size: 14px;
  }
}
```

---

## ۲. ارگونومی لمسی و ناحیه شست (Thumb Zone & Ergonomics)

### ۲.۱ حداقل ابعاد اهداف لمسی (Touch Target Size)
- **حداقل مساحت تعاملی:** هر دکمه، تب، سوییچ یا سطر قابل‌کلیک باید حداقل `44×44px` (استاندارد Apple HIG) یا `48×48px` (Material Design) مساحت لمسی داشته باشد، حتی اگر آیکون یا متن داخل آن کوچک‌تر است (استفاده از padding یا pseudo-elements).
- **فاصله بین دکمه‌ها:** حداقل `8px` فضای خالی بین دو هدف لمسی مجاور جهت جلوگیری از خطای تاچ (Mis-tap).

### ۲.۲ چیدمان مبتنی بر دسترس‌پذیری شست
- **ناحیه امن (Natural Thumb Zone):** نیمه پایینی نمایشگر. تمام اقدامات پرتکرار و حیاتی (ثبت حضور، ارسال تکلیف، ذخیره، تایید، فیلتر) باید در نیمه پایین یا در **Bottom Sticky Bar** قرار گیرند.
- **ناحیه پرخطر (Hard to Reach):** بالای نمایشگر. اقدامات خطرناک یا کم‌تکرار (خروج از حساب، حذف نهایی با دیالوگ تایید، تنظیمات سیستمی) در این بخش قرار می‌گیرند.

### ۲.۳ معماری ناوبری بر اساس نقش (Navigation Architecture)
- **معلمان، دانش‌آموزان و اولیاء:** ناوبری اصلی از طریق **Bottom Navigation Bar** با حداکثر ۴ تا ۵ گزینه کلیدی با برچسب و آیکون انجام می‌شود.
- **مدیران و سوپرادمین‌ها:** در دسکتاپ از Sidebar تعاملی و در موبایل از یک منوی کشویی روان (Off-canvas Drawer) با کلید همبرگری بزرگ و پشتیبانی از ژست Swipe استفاده می‌شود.

---

## ۳. تایپوگرافی فارسی، راست‌به‌چپ (RTL) و ارقام

### ۳.۱ استانداردهای تایپوگرافی
- **فونت رسمی:** خانواده وزیرمتن (`Vazirmatn`) با وزن‌های Regular (۴۰۰)، Medium (۵۰۰)، Bold (۷۰۰) و Black (۹۰۰).
- **حداقل اندازه متن بدنه روی موبایل:** `16px` (`text-base`). متن‌های ریزتر از `14px` تنها برای برچسب‌های کمکی مجاز هستند؛ فونت فارسی به دلیل وجود نقطه‌ها و اعراب، در اندازه‌های زیر ۱۴px ناخوانا می‌شود.
- **ارتفاع خط (`line-height`):** حداقل `1.6` تا `1.7` برای جلوگیری از تداخل اعراب با سطر بالایی و پایینی.

### ۳.۲ منطق راست‌به‌چپ (RTL) و CSS Logical Properties
پروژه کاملاً بر پایه `dir="rtl"` استوار است. هیچ توسعه‌دهنده‌ای مجاز به استفاده از کلاس‌های جهت‌دار فیزیکی هاردکد (`ml-`, `mr-`, `left-`, `right-`, `pl-`, `pr-`) نیست:
- به جای `mr-4` از `ms-4` (Margin Inline Start) استفاده شود.
- به جای `ml-4` از `me-4` (Margin Inline End) استفاده شود.
- به جای `pl-4` / `pr-4` از `ps-4` / `pe-4` استفاده شود.
- آیکون‌های جهت‌دار (فلش برگشت، ناوبری صفحات، چورون‌ها) در زبان‌های RTL باید قرینه شوند (Mirroring).

### ۳.۳ فرمت و تعامل با ارقام و اعداد
- **لایه نمایش (UI Output):** تمامی ارقام نمایش داده شده به کاربر (تاریخ، مبالغ، ساعات، تعداد، نمرات) طبق متدهای کمکی با ارقام فارسی (`۰۱۲۳۴۵۶۷۸۹`) رندر می‌شوند.
- **لایه ورودی (Input Fields):** در فیلدهای ورودی عددی (نمره، تلفن، کدملی، کد پستی) خصیصه `inputmode="numeric"` اجباری است تا کیبورد عددی باز شود؛ سیستم با متد `normalizePersianDigits` هر دو حالت ارقام فارسی و انگلیسی را پذیرفته و پردازش می‌کند.

---

## ۴. استانداردهای فرم‌ها و ورود اطلاعات

### ۴.۱ فرم‌های تک‌ستونه در موبایل
- در موبایل تمام فرم‌ها تک‌ستونه و عمودی (`flex flex-col gap-4`) هستند؛ چینش چندستونه فقط از `md:` به بالا فعال می‌شود.
- فیلدهای مرتبط در دسته‌بندی‌های بصری مشخص (Group Cards) با سربرگ واضح قرار می‌گیرند.

### ۴.۲ دکمه‌های اقدام چسبان (Sticky Action Bars)
در فرم‌های طولانی (ثبت‌نام، ایجاد آزمون، تعریف مشخصات مدرسه):
- دکمه ثبت نباید در انتهای طومار اسکرول پنهان باشد.
- دکمه ارسال باید به شکل `sticky bottom-0 bg-white/90 backdrop-blur-md border-t p-4 z-20` همواره جلوی چشم و زیر انگشت شست کاربر باشد.

### ۴.۳ اعتبارسنجی درجا (Inline Real-time Validation)
- اعتبارسنجی هنگام خروج از فیلد (`onBlur`) یا پس از اولین ارسال فعال می‌شود.
- پیام خطا دقیقاً در زیر فیلد با رنگ قرمز واضح و آیکون هشدار نمایش داده می‌شود (نه صرفاً در یک اعلان Toast بالای صفحه که زیر کیبورد یا خارج از دید باشد).
- فیلد خطا خورده فوکوس واضح گرفته و به طور خودکار در دید اسکرول می‌شود (`scrollIntoView`).

### ۴.۴ انتخابگر تاریخ شمسی (Jalali Date Picker)
- طبق سند [jalalidate.md](file:///Users/behrad/Desktop/Projects/rokad-platform/jalalidate.md)، در موبایل انتخابگر تاریخ به صورت **Bottom Sheet** باز می‌شود.
- امکان جابه‌جایی سریع بین ماه‌ها و سال‌ها با چرخ یا دراپ‌دان لمسی فراهم است و کلیدهای میانبر («امروز»، «فردا»، «هفته بعد») برای دسترسی تک‌لمسی تعبیه شده‌اند.

---

## ۵. جداول داده و قانون طلایی تبدیل به Card List

> [!IMPORTANT]
> **قانون بدون استثنا:** در هیچ کجای فرانت‌اند رُکاد مجاز نیستیم جداول داده را در موبایل به صورت رها شده با اسکرول افقی خام (`overflow-x-auto`) رها کنیم.

### ۵.۱ معماری کامپوننت `MobileDataTable`
هر لیست داده یا جدولی از الگوی دوگانه پیروی می‌کند:
- **در موبایل (`< md`):** به صورت **کارت‌های مستقل لمسی (Card List)** رندر می‌شود. هر کارت شامل اطلاعات حیاتی در دید اولیه، و جزییات فرعی در یک کشوی آکاردئونی (Disclosure/Accordion) است.
- **در تبلت و دسکتاپ (`≥ md`):** به جدول ماتریسی استاندارد با قابلیت مرتب‌سازی، ستون‌های کامل و صفحه‌بندی تبدیل می‌شود.

```
+------------------------------------+
| موبایل (< 768px): لیست کارتی       |
| +--------------------------------+ |
| | [آواتار] علیرضا کاظمی          | |
| | کد دانش‌آموزی: ۱۲۳۴۵            | |
| | وضعیت: [حاضر]    نمره: ۱۸.۵    | |
| | [مشاهده جزییات و سوابق v]      | |
| +--------------------------------+ |
+------------------------------------+
                   |
                   v (در تبلت و دسکتاپ)
+------------------------------------+
| نام و نام خانوادگی | کد | حضور | نمره  |
| علیرضا کاظمی       | ۱۲ | حاضر | ۱۸.۵ |
+------------------------------------+
```

### ۵.۲ استثنای جداول ماتریسی دو بعدی (مانند برنامه هفتگی)
برنامه هفتگی مدارس که ارتباط دوبعدی «روز × زنگ» را نشان می‌دهد، با اسکرول افقی جدول حل نمی‌شود؛ بلکه:
- در موبایل به صورت **تب‌های افقی روزهای هفته** (شنبه، یکشنبه، دوشنبه...) با کشیدن انگشت (Swipe) یا کلیک تب در بالا، و نمایش کارتی اسلات‌های آن روز در زیر آن ارائه می‌شود.
- در دسکتاپ نمای کامل ماتریس ۶ روز در ۵ زنگ به نمایش درمی‌آید.

---

## ۶. مشخصات UX به تفکیک نقش‌های کاربری

### ۶.۱ میز کار معلم و دبیر (Teacher Cockpit)
- **دفتر کلاسی یکپارچه (Single Hub Classroom Logbook):**
  - ادغام کامل حضور و غیاب، نمرات پرسش کلاسی و امور انضباطی/تشویقی در یک صفحه واحد.
  - تعویض وضعیت حضور هر هنرجو تنها با یک لمس روی کلیدهای چهارگانه (حاضر سبز، غایب قرمز، موجه نارنجی، تاخیر آبی).
  - کلیدهای سریع نمره‌دهی شفاهی (۲۰، ۱۸، ۱۵) با یک تپ برای تسریع کار بدون نیاز به باز شدن کیبورد.
  - کلیدهای سریع ثبت تشویق (+۱) و تذکر (-۱) با ثبت آنی در پرونده و اطلاع‌رسانی پیامکی به اولیاء.
- **تکالیف و تصحیح:**
  - امکان مشاهده پاسخ‌های متنی و تصاویر ارسالی دانش‌آموزان با یک پیش‌نمایش تمام‌صفحه و ثبت نمره/بازخورد صوتی و متنی.

### ۶.۲ پرتال دانش‌آموز (Student Portal)
- **داشبورد تحصیلی متمرکز:**
  - نمایش تکالیف سررسیددار امروز در بالاترین نقطه کارت‌های صفحه.
  - دسترسی مستقیم با یک کلیک به «ارسال پاسخ تکلیف» (بدون نیاز به جستجو در منوها).
  - برنامه درسی همان روز به صورت پررنگ همراه با ساعت شروع زنگ بعد.
- **کارنامه و نمرات:**
  - نمایش نمرات با نمودار میله‌ای شفاف و نمرات تفکیک‌شده ماهانه و ترمی؛ رتبه‌بندی محرمانه بدون القای حس سرشکستگی.

### ۶.۳ پرتال اولیاء (Parent Portal)
- **اطلاع‌رسانی بلادرنگ وضعیت فرزند:**
  - هشدارهای غیبت یا تاخیر در همان دقایق اول زنگ با نشانگر قرمز برجسته.
  - ثبت نمرات جدید و گزارش‌های انضباطی با تفکیک لحن تشویقی (سبز) و نیازمند پیگیری (زرد/قرمز).
- **رزرو ملاقات با معلم:**
  - تقویم انتخاب اسلات‌های آزاد دبیران بدون نیاز به تماس تلفنی با مدرسه.
- **امور مالی و اقساط:**
  - نمایش خلاصه وضعیت بدهی/بستانکاری و کلید تک‌مرحله‌ای «پرداخت قسط شهریه» متصل به درگاه شاپرک.

### ۶.۴ مدیریت و کادر اداری مدرسه (School Administration)
- **مدیریت ساختار و کلاس‌ها:**
  - فیلترهای موبایلی سریع از طریق **Filter Bottom Sheet** کشویی.
  - ثبت سریع دانش‌آموز با بارگذاری اکسل یا ورود تک‌به‌تک با فرم استاندارد موبایل.
- **گزارش‌ها:**
  - نمودارهای واکنشی (Responsive Charts) که روی موبایل با اسکرول افقی محتوا یا فشرده‌سازی بازه نمایش داده می‌شوند.

---

## ۷. سیستم بازخورد، وضعیت‌ها و لودینگ (Feedback & States)

### ۷.۱ صفحات و اسکلت‌های بارگذاری (Skeleton Screens)
- استفاده از اسپینرهای چرخان تمام‌صفحه (Full-page Spinners) اکیداً ممنوع است.
- به جای آن، برای هر بخش از اسکلت بارگذاری متناظر (`Skeleton`) با همان ابعاد کارت یا جدول استفاده می‌شود تا کاربر پیش‌نمایش ساختار صفحه را حس کند.

### ۷.۲ وضعیت‌های خالی (Empty States)
هیچ لیستی نباید سفید یا بی‌روح رها شود:
- هر Empty State شامل: یک آیکون/تصویرگری خطی متناسب، عنوان کوتاه و صمیمی، متن راهنمای یک‌خطی، و یک **دکمه اقدام اولیه (Call to Action)** است (مثلاً: «هنوز تکلیفی برای این کلاس تعریف نشده است» + دکمه «تعریف اولین تکلیف»).

### ۷.۳ اعلان‌ها و بازخورد عملیات (Toasts & Alerts)
- پیام‌های موفقیت یا خطای عملیاتی به صورت Toast شناور در پایین صفحه (روی موبایل) یا گوشه صفحه (روی دسکتاپ) با مدت زمان حداکثر ۴ ثانیه نمایش داده می‌شوند.
- در عملیات حساس (مانند حذف دانش‌آموز، لغو کلاس)، همیشه عملیات لغو سریع (`Undo`) یا دیالوگ تایید دومرحله‌ای با توضیح عواقب تعبیه می‌شود.

---

## ۸. تاب‌آوری در اتصال ضعیف و قابلیت آفلاین (Offline-First)

با توجه به عدم دسترسی به اینترنت پایدار در زیرزمین‌ها و کارگاه‌های برخی هنرستان‌ها:
- **فرم‌های حیاتی (به ویژه دفتر کلاسی و حضور و غیاب):** داده‌های ثبت‌شده ابتدا در حافظه محلی مرورگر (`IndexedDB` / `localStorage`) ذخیره می‌شوند (Optimistic UI).
- **همگام‌سازی پس‌زمینه (Background Sync):** به محض برقرار شدن اتصال اینترنت، درخواست‌ها به طور خودکار به سرور ارسال شده و برچسب وضعیت از «ذخیره محلی» به «همگام‌شده با سرور» تغییر می‌یابد.
- **نشانگر اتصال:** نوار باریک هشدار در بالای صفحه تنها در زمان قطعی کامل شبکه نمایان می‌شود و به کاربر اطمینان می‌دهد که اطلاعاتش گم نخواهد شد.

---

## ۹. دسترس‌پذیری (Accessibility - WCAG 2.1 AA)

- **کنتراست رنگ‌ها:** نسبت کنتراست متن به پس‌زمینه حداقل `4.5:1` برای متون عادی و `3:1` برای متون درشت و عناصر تعاملی.
- **عدم اتکای صرف به رنگ:** هیچ وضعیتی (حاضر/غایب، قبولی/مردودی، تشویق/تذکر) فقط با رنگ متمایز نمی‌شود؛ بلکه باید همراه با آیکون معنادار و متن فارسی صریح باشد.
- **پشتیبانی از صفحه‌خوان‌ها:** کلیه دکمه‌های آیکونی فاقد متن باید دارای اتریبیوت `aria-label` فارسی گویا باشند (مانند `aria-label="بستن پنجره"` یا `aria-label="ثبت تشویق هنرجو"`).

---

## ۱۰. ماتریس کنترل کیفیت بصری (Visual QA)

پیش از ارسال هر Pull Request یا ادغام کد جدید، توسعه‌دهنده موظف است کامپوننت یا صفحه را در ابعاد زیر تست نماید:

| عرض Viewport | نماینده سخت‌افزاری | موارد کنترلی اجباری |
|---|---|---|
| **`360px`** | اندروید پایه و اقتصادی | عدم وجود هرگونه اسکرول ناخواسته افقی، چینش درست کارت‌ها، خوانایی فونت ۱۶px، دکمه‌های لمسی حداقل ۴۴px |
| **`390px`** | آیفون استاندارد | تعامل روان با انیمیشن‌ها، عدم تداخل دکمه چسبان با نوار Home Indicator سیستم‌عامل |
| **`768px`** | تبلت عمودی / iPad | تغییر حالت کارت به جدول در صورت نیاز، عملکرد درست دیالوگ‌ها و منوهای کشویی |
| **`1024px+`** | دسکتاپ و مانیتور اداری | سایدبار ثابت، تراز بودن جداول عریض، بهره‌وری از فضای افقی بدون کشیدگی نامطلوب فرم‌ها |

---

## ۱۱. چک‌لیست حاکمیتی پیاده‌سازی و بازبینی کد

- [ ] طراحی از موبایل ۳۶۰px آغاز شده و گسترش با `min-width` انجام گرفته است؟
- [ ] هیچ جدول داده‌ای بدون تبدیل به Card List در موبایل رها نشده است؟
- [ ] ابعاد تمامی عناصر لمسی حداقل `44×44px` و فاصله آن‌ها از هم حداقل `8px` است؟
- [ ] عملیات پرتکرار و اصلی در منطقه دسترس شست (نیمه پایین صفحه یا نوار چسبان) قرار دارند؟
- [ ] تایپوگرافی با وزیرمتن، بدون فونت‌های زیر ۱۴px برای متن بدنه و با `line-height >= 1.6` است؟
- [ ] استایل‌ها با قوانین منطقی RTL (`ms-`, `me-`, `ps-`, `pe-`) نوشته شده و از `ml-` و `mr-` پرهیز شده است؟
- [ ] اعداد در نمایش با ارقام فارسی و در ورودی‌ها با `inputmode="numeric"` و سازگار با دو رقم هستند؟
- [ ] فرم‌ها در موبایل تک‌ستونه بوده و دکمه ذخیره در فرم‌های طولانی چسبان (`sticky`) است؟
- [ ] تاریخ‌ها از انتخابگر تقویم جلالی سازگار با موبایل (Bottom Sheet) استفاده می‌کنند؟
- [ ] اسکلت بارگذاری (`Skeleton`) و وضعیت خالی با دکمه اقدام (`Empty State`) تعریف شده است؟